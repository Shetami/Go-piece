import type { Quiz } from '../../../components/quiz/types.ts'

export default {
  questions: [
    {
      kind: 'single',
      q: '`ALTER TABLE orders ADD COLUMN note text` выполняется за миллисекунды, но во время миграции все запросы к `orders` встали на десять минут. Что вероятнее всего произошло?',
      options: [
        'Миграция переписывала таблицу',
        'Миграция ждала блокировку за долгим запросом, и все новые запросы встали в очередь за ней',
        'Закончилось место на диске',
        'Автовакуум заблокировал таблицу',
      ],
      answer: 1,
      explain:
        'Запрос `ACCESS EXCLUSIVE` в очереди блокирует всех, кто пришёл после него, даже простые `SELECT`. Защита — `lock_timeout` на миграциях и отсутствие долгих транзакций на основной базе.',
    },
    {
      kind: 'multi',
      q: 'Какие операции на большой таблице держат её недоступной долго, если выполнить их «в лоб»?',
      options: [
        '`ALTER COLUMN id TYPE bigint`',
        '`ADD COLUMN flag boolean DEFAULT false`',
        '`ALTER COLUMN email SET NOT NULL`',
        '`CREATE INDEX` без `CONCURRENTLY`',
        '`ADD COLUMN note text`',
      ],
      answer: [0, 2, 3],
      explain:
        'Смена типа переписывает таблицу и индексы, `SET NOT NULL` просматривает все строки под исключительной блокировкой, обычный `CREATE INDEX` блокирует запись на всё время построения. Добавление колонки без умолчания или с постоянным умолчанием меняет только каталог.',
    },
    {
      kind: 'truefalse',
      q: 'Если `CREATE INDEX CONCURRENTLY` упал посреди работы, в базе не остаётся никаких следов, и его можно просто запустить снова.',
      answer: false,
      explain:
        'Остаётся индекс в состоянии `INVALID`: читать по нему нельзя, а обновляется он при каждой записи. Его нужно найти через `pg_index.indisvalid` и удалить перед повтором.',
    },
    {
      kind: 'single',
      q: 'Первичный ключ — `serial` (int4), вставок — 300 в секунду. Когда он закончится?',
      options: ['Примерно через 80 дней', 'Примерно через 8 лет', 'Примерно через 3 дня', 'Никогда: тип расширится автоматически'],
      answer: 0,
      explain:
        '2,1 миллиарда / (300 × 86 400) ≈ 83 дня, а с откаченными транзакциями и неудачными вставками — раньше. Перевод на `bigint` потом — это переписывание таблицы, поэтому ключ делают `bigint` сразу.',
    },
    {
      kind: 'truefalse',
      q: 'Страница ленты с `OFFSET 100000` стоит заметно дороже первой, даже если по колонке сортировки есть индекс.',
      answer: true,
      explain:
        'B-дерево не умеет пропускать N записей: база читает все сто тысяч и выбрасывает их. Пагинация по ключу продолжает с последней увиденной строки, и каждая страница стоит как первая.',
    },
    {
      kind: 'single',
      q: 'Запрос быстрый в `psql` и медленный из сервиса на pgx с теми же значениями параметров. Что проверить первым?',
      options: [
        'Сетевую задержку до базы',
        'Общий план подготовленного запроса: после пяти выполнений база могла перейти на план без учёта значений',
        'Версию драйвера pgx',
        'Размер пула соединений',
      ],
      answer: 1,
      explain:
        'pgx готовит и кэширует запросы на соединении, а PostgreSQL после пяти выполнений может выбрать общий план. На неравномерных данных он бывает в разы хуже. Лечится `plan_cache_mode = force_custom_plan` или частичным индексом.',
    },
    {
      kind: 'single',
      q: 'Пул — 25 соединений на под, `max_connections` = 400. До скольких подов можно масштабировать сервис, не упираясь в предел базы (с запасом на другие сервисы и администратора)?',
      options: ['Примерно до 14', 'Примерно до 40', 'Сколько угодно: PostgreSQL поставит лишние соединения в очередь', 'Примерно до 100'],
      answer: 0,
      explain:
        '400 / 25 = 16 подов — это уже весь предел без остатка. Нужен запас на другие сервисы, миграции и резерв суперпользователя. PostgreSQL лишние соединения не ставит в очередь, а отказывает.',
    },
    {
      kind: 'order',
      q: 'Как безопасно сделать колонку `email` обязательной на большой таблице? Расставьте шаги.',
      items: [
        'Заполнить пустые значения пачками',
        '`ADD CONSTRAINT email_nn CHECK (email IS NOT NULL) NOT VALID`',
        '`VALIDATE CONSTRAINT email_nn` — проверка под слабой блокировкой',
        '`ALTER COLUMN email SET NOT NULL` — без полного просмотра',
        '`DROP CONSTRAINT email_nn` — он больше не нужен',
      ],
      explain:
        'Ограничение `NOT VALID` добавляется мгновенно, проверка идёт под блокировкой, которая не мешает чтению и записи, а `SET NOT NULL` видит проверенное ограничение и не просматривает таблицу.',
    },
  ],
} satisfies Quiz
