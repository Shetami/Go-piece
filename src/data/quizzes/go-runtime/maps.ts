import type { Quiz } from '../../../components/quiz/types.ts'

export default {
  questions: [
    {
      kind: 'single',
      q: 'Что хранит контрольный байт занятого слота в Swiss table?',
      options: [
        'Индекс следующего слота в цепочке',
        'Младшие 7 бит хэша ключа (H2), старший бит — 0',
        'Старшие 8 бит хэша ключа (`tophash`)',
        'Счётчик обращений для вытеснения',
      ],
      answer: 1,
      explain:
        'Старший бит отличает занятый слот от `empty` (0x80) и `deleted` (0xfe). `tophash` из старших 8 бит — это устройство карт до Go 1.24.',
    },
    {
      kind: 'single',
      q: 'Поиск ключа проверил группу: совпадений по H2 нет, в группе есть слот `empty`. Что дальше?',
      options: [
        'Перейти к следующей группе по последовательности проб',
        'Остановиться: ключа в таблице нет',
        'Проверить все группы таблицы подряд',
        'Перейти к следующей таблице в каталоге',
      ],
      answer: 1,
      explain:
        'Вставка кладёт ключ в первое свободное место на его последовательности проб. Группа с пустым слотом значит, что дальше неё ключ уйти не мог. Надгробие (`deleted`) такого права не даёт — через него поиск идёт дальше.',
    },
    {
      kind: 'single',
      q: 'Зачем при удалении из полной группы слот помечается надгробием, а не `empty`?',
      options: [
        'Чтобы итератор не вернул удалённый ключ',
        'Пустой слот оборвал бы поиск ключей, которые при вставке прошли эту группу дальше',
        'Чтобы освободить память при следующем GC',
        'Надгробие хранит старое значение для отката',
      ],
      answer: 1,
      explain:
        'Пока группа была полной, ключи с той же начальной группой уходили дальше по последовательности проб. `empty` в ней заставил бы их поиск остановиться раньше времени. Если в группе уже есть `empty`, надгробие не нужно.',
    },
    {
      kind: 'order',
      q: 'Расставьте, что делает вставка, когда у таблицы кончился запас `growthLeft`.',
      items: [
        'Попробовать пропалывать надгробия, если их больше 10% ёмкости',
        'Если места не прибавилось и таблица меньше 1024 слотов — удвоить её',
        'Если таблица уже на 1024 слота — поделить её на две по следующему биту хэша',
        'Если каталогу не хватает бит для новых таблиц — удвоить каталог',
      ],
      explain:
        'Прополка дешевле роста, но запускается, только если вернёт хотя бы 10% места. Удвоение каталога — часть деления: это массив указателей, он дешёвый.',
    },
    {
      kind: 'single',
      q: 'В карте был миллион записей, осталось десять. Сколько стоит `for range` по ней?',
      options: [
        'Как обход десяти записей: итератор идёт только по живым',
        'Как обход всей ёмкости: итератор проверяет каждый слот, а карта не уменьшилась',
        'Ноль: пустые таблицы итератор пропускает целиком',
      ],
      answer: 1,
      explain:
        'Обход стоит O(ёмкости). На Go 1.27 это около 800 мкс против 64 нс у свежей карты на те же десять ключей. После сильного сжатия записи перекладывают в новую карту.',
    },
    {
      kind: 'multi',
      q: 'Что верно про обход карты через `range`?',
      options: [
        'Порядок намеренно случаен и может отличаться между двумя циклами в одной программе',
        'Удалять ключи той же карты внутри цикла безопасно',
        'Ключ, добавленный внутри цикла, гарантированно будет обойдён',
        'Если карта выросла во время обхода, ни одна запись не вернётся дважды',
      ],
      answer: [0, 1, 3],
      explain:
        'Добавленный ключ может попасть в обход, а может и нет. При росте итератор продолжает выбирать ключи из старой таблицы, а значения перечитывает в новой — так и соблюдается «не больше одного раза».',
    },
    {
      kind: 'truefalse',
      q: '`delete` всех ключей или `clear(m)` возвращают память, занятую картой.',
      answer: false,
      explain:
        'Карта не уменьшается: ни `delete`, ни `clear` не освобождают группы, таблицы и каталог. Освобождаются только объекты, на которые ссылались ключи и значения. Вернуть память — создать новую карту.',
    },
    {
      kind: 'single',
      q: 'Кэш на 50 миллионов записей, GC съедает заметную долю CPU. Какая замена ключа сильнее всего разгрузит сборщик?',
      options: [
        '`map[string]int64` → `map[[16]byte]int64` (ключ — хэш или UUID фиксированной длины)',
        '`map[string]int64` → `map[string]*int64`',
        '`map[string]int64` → `sync.Map`',
        'Добавить `make(map[string]int64, 50_000_000)`',
      ],
      answer: 0,
      explain:
        'Строка — это указатель, и разметка обходит каждую. Карта без указателей в ключах и значениях выделяется как `noscan`, сборщик в неё не заходит. На 5 млн записей это 0,35 мс против 13,7 мс на цикл GC.',
    },
    {
      kind: 'multi',
      q: 'Для каких карт компилятор выберет быстрый путь (`mapaccess1_fast*`)?',
      options: [
        '`map[string]int`',
        '`map[struct{ A, B int32 }]int`',
        '`map[[16]byte]int`',
        '`map[int64][256]byte`',
      ],
      answer: [0, 1],
      explain:
        'Быстрые пути — для ключей в 4 или 8 байт, указателей и строк, и только если значение не больше 128 байт. Две `int32` — 8 байт, `fast64`. `[16]byte` — 16 байт, общий путь; значение в 256 байт — тоже общий путь.',
    },
    {
      kind: 'single',
      q: 'Две горутины одновременно пишут в одну карту, и рантайм это заметил. Что будет?',
      options: [
        'Паника, которую можно перехватить `recover`',
        '`fatal error: concurrent map writes` — процесс завершается, `recover` и `defer` не помогают',
        'Одна из записей тихо потеряется',
        'Вторая горутина подождёт на внутреннем мьютексе карты',
      ],
      answer: 1,
      explain:
        'Это fatal error: внутренние структуры могли быть повреждены. Флаг `writing` переключается XOR, чтобы гонку заметил хотя бы один из писателей, но проверка без атомарных операций ловит не всё — нужен `-race`.',
    },
  ],
} satisfies Quiz
