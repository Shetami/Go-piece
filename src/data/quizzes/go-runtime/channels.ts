import type { Quiz } from '../../../components/quiz/types.ts'

export default {
  questions: [
    {
      kind: 'multi',
      q: 'Что входит в структуру `hchan`?',
      options: [
        'Кольцевой буфер со счётчиками `sendx` и `recvx`',
        'Очереди ждущих горутин `recvq` и `sendq`',
        'Мьютекс `lock`',
        'Отдельный поток ОС для доставки значений',
        'Список всех горутин, когда-либо писавших в канал',
      ],
      answer: [0, 1, 2],
      explain:
        'Буфер, две очереди ожидания и мьютекс — всё поведение канала следует из этих полей. Отдельных потоков у канала нет, а каждая операция берёт `c.lock`: канал не lock-free.',
    },
    {
      kind: 'single',
      q: 'Получатель уже спит на небуферизованном канале, приходит отправитель. Как значение попадает к получателю?',
      options: [
        'Отправитель кладёт его в буфер, получатель просыпается и забирает',
        'Отправитель копирует значение прямо в стек получателя и будит его',
        'Получатель просыпается и сам копирует значение из стека отправителя',
        'Значение копируется в кучу, получатель получает указатель',
      ],
      answer: 1,
      explain:
        'Работу делает пришедший вторым: один `memmove` из стека в стек через поле `elem` у sudog и `goready`. Разбуженная горутина кладётся в `runnext` — значение ещё в кэше этого ядра.',
    },
    {
      kind: 'truefalse',
      q: 'Увеличение буфера канала увеличивает пропускную способность медленного получателя.',
      answer: false,
      explain:
        'Получатель обработает столько, сколько успевает, при любом буфере. Буфер только даёт отправителю убежать вперёд на `n` значений и откладывает обратное давление. Слишком большой буфер эту защиту выключает: всплеск превращается в рост памяти и задержки.',
    },
    {
      kind: 'single',
      q: 'В конвейере буфер перед стадией B всё время полон, а буфер после неё почти пуст. Что это значит?',
      options: [
        'Узкое место — стадия B',
        'Узкое место — источник данных',
        'Узкое место — приёмник',
        'Буферы слишком маленькие',
      ],
      answer: 0,
      explain:
        'Полный буфер перед стадией и пустой после неё указывают прямо на узкое место — без профилировщика, по одному `len(ch)`. Ускорить можно саму стадию или добавить на неё воркеров, пока потолком не станет источник.',
    },
    {
      kind: 'order',
      q: 'Расставьте проходы блокирующего `select` без `default`.',
      items: [
        'Обойти case в случайном порядке и проверить, готов ли кто-то прямо сейчас',
        'Никто не готов — завести по sudog на каждый case и встать во все очереди',
        'Уснуть один раз через `gopark`',
        'Проснуться от одного канала и сняться с очередей остальных',
      ],
      explain:
        'Поэтому ожидание десяти каналов стоит десять постановок в очередь и один сон, а не опрос в цикле. С `default` второй и третий проходы не наступают: не готово — сразу в default.',
    },
    {
      kind: 'truefalse',
      q: 'Если в `select` готовы несколько case, выполняется случайный из них.',
      answer: true,
      explain:
        'Выбирается случайный готовый case — это требование спецификации. Иначе первый case навсегда заглушал бы остальные (например, `jobs` не давал бы сработать `quit`). Приоритет, если он нужен, делают вложенным `select` с `default`.',
    },
    {
      kind: 'multi',
      q: 'Какие операции с каналом приводят к панике?',
      options: [
        'Отправка в закрытый канал',
        'Повторное закрытие канала',
        '`close` у nil-канала',
        'Приём из закрытого канала с пустым буфером',
        'Приём из nil-канала',
      ],
      answer: [0, 1, 2],
      explain:
        'Приём из закрытого канала возвращает нулевое значение и `ok == false` сразу и сколько угодно раз. Приём из nil-канала не паникует, а блокирует навсегда: очереди у nil-канала нет.',
    },
    {
      kind: 'single',
      q: 'Кто должен закрывать канал?',
      options: [
        'Получатель — когда ему хватит данных',
        'Отправитель, и только один',
        'Любая горутина, предварительно проверив, что канал ещё открыт',
        'Никто: каналы собирает сборщик мусора',
      ],
      answer: 1,
      explain:
        'Проверить «открыт ли канал» перед отправкой нельзя: между проверкой и отправкой другая горутина успеет закрыть канал. Поэтому закрывает единственный отправитель, а получатель, которому «хватит», сообщает об этом через отдельный канал или контекст.',
    },
    {
      kind: 'single',
      q: 'Воркер отправляет результат в небуферизованный канал, а вызывающий уже ушёл по таймауту. Каков канонический фикс?',
      options: [
        'Закрыть канал в вызывающем после таймаута',
        'Сделать буфер ровно на число ожидаемых ответов, например `make(chan Result, 1)`',
        'Проверять `len(ch)` перед отправкой',
        'Добавить `runtime.Gosched()` в воркер',
      ],
      answer: 1,
      explain:
        'С буфером отправка завершается и без получателя, и воркер доходит до конца. Без него горутина висит в `chan send` вечно — утечка по горутине на запрос. `checkdead` её не заметит: он срабатывает, только когда спят все.',
    },
    {
      kind: 'truefalse',
      q: 'Всё, что горутина записала в память до `close(done)`, гарантированно видно горутине, которая дождалась `<-done`.',
      answer: true,
      explain:
        'Каналы входят в модель памяти Go: закрытие происходит раньше приёма нулевого значения, отправка — раньше завершения приёма. Поэтому канал публикует не только значение, но и всю память, записанную до передачи.',
    },
  ],
} satisfies Quiz
