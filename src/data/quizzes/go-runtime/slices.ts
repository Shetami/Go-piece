import type { Quiz } from '../../../components/quiz/types.ts'

export default {
  questions: [
    {
      kind: 'single',
      q: 'Что на самом деле хранится в переменной типа `[]int` с миллионом элементов?',
      options: [
        'Сам миллион чисел',
        'Указатель на массив, длина и ёмкость — три машинных слова',
        'Только указатель на массив; длина хранится в массиве',
        'Указатель на связный список блоков',
      ],
      answer: 1,
      explain:
        'Слайс — это заголовок из трёх слов, элементы лежат в отдельном массиве. Поэтому присваивание и передача в функцию копируют 24 байта, а массив остаётся общим.',
    },
    {
      kind: 'single',
      q: '`s := []int{1, 2, 3}`, функция `f(s)` делает `s[0] = 9; s = append(s, 4)`. Что увидит вызывающий в `s` после вызова?',
      options: ['`[1 2 3]`', '`[9 2 3]`', '`[9 2 3 4]`', '`[1 2 3 4]`'],
      answer: 1,
      explain:
        'Запись `s[0]` идёт в общий массив и видна снаружи. Длина же лежит в заголовке вызывающего, и `append` внутри функции её не меняет — тем более что у литерала нет запаса ёмкости, и `append` переехал в новый массив.',
    },
    {
      kind: 'single',
      q: '`base := make([]int, 3, 10); a := append(base, 1); b := append(base, 2)`. Чему равно `a[3]`?',
      options: ['1', '2', '0', 'Паника: индекс за пределами'],
      answer: 1,
      explain:
        'Ёмкости хватает, оба `append` пишут в одну и ту же ячейку общего массива. Последней записана двойка — её и видят оба слайса.',
    },
    {
      kind: 'single',
      q: 'Зачем писать `return buf[:n:n]` вместо `return buf[:n]`?',
      options: [
        'Так быстрее: компилятор убирает проверку границ',
        'Ёмкость результата равна длине, и `append` вызывающего гарантированно переедет, не затерев `buf[n:]`',
        'Так результат становится неизменяемым',
        'Так данные копируются в новый массив',
      ],
      answer: 1,
      explain:
        'Третий индекс ограничивает ёмкость. Копии не происходит и запись по индексу по-прежнему идёт в `buf`, но `append` к такому слайсу уже не может писать за границу `n`.',
    },
    {
      kind: 'multi',
      q: 'Какие утверждения о росте ёмкости верны?',
      options: [
        'До ёмкости 256 при нехватке места ёмкость удваивается',
        'После 256 коэффициент плавно снижается к 1,25',
        'Размер нового массива округляется до класса размера аллокатора',
        'Ёмкость после каждого `append` строго предсказуема по коду и не зависит от версии Go',
      ],
      answer: [0, 1, 2],
      explain:
        'Формула менялась (до Go 1.18 порог был 1024), а свежие компиляторы начинают растущий в цикле слайс в буфере на стеке. Код не должен полагаться на конкретную ёмкость.',
    },
    {
      kind: 'single',
      q: 'Кэш хранит `body[:64]` от ответов по 2 МБ. Как избежать удержания памяти?',
      options: [
        'Вызвать `runtime.GC()` после записи в кэш',
        'Хранить `bytes.Clone(body[:64])` — копию в собственном маленьком массиве',
        'Хранить `body[:64:64]`',
        'Обнулить `body` после записи в кэш',
      ],
      answer: 1,
      explain:
        'Любой заголовок, указывающий внутрь массива, держит живым весь массив. Трёхиндексный срез ограничивает ёмкость, но указывает в тот же массив. Помогает только копия.',
    },
    {
      kind: 'truefalse',
      q: '`encoding/json` сериализует nil-слайс и пустой слайс `[]int{}` одинаково.',
      answer: false,
      explain:
        'nil-слайс становится `null`, пустой — `[]`. Во всём остальном — `len`, `range`, `append` — они ведут себя одинаково, поэтому пустоту проверяют через `len(s) == 0`.',
    },
    {
      kind: 'multi',
      q: 'Что безопасно делать из нескольких горутин без синхронизации?',
      options: [
        'Писать в разные индексы заранее созданного слайса `results[i] = ...`',
        '`append` в общий слайс',
        'Одновременно читать один слайс, который никто не меняет',
        'Одной горутине делать `append`, другим — читать `len(s)`',
      ],
      answer: [0, 2],
      explain:
        'Разные элементы — разная память, гонки нет. `append` читает и пишет общий заголовок — это гонка, и элементы теряются без паники. Чтение `len` параллельно с записью заголовка — тоже гонка.',
    },
    {
      kind: 'order',
      q: 'Расставьте шаги `append(s, x)`, когда ёмкости не хватает.',
      items: [
        'Вычислить новую ёмкость по `nextslicecap`',
        'Округлить размер до класса размера аллокатора',
        'Выделить новый массив через `mallocgc`',
        'Скопировать старые элементы `memmove`',
        'Записать `x` и вернуть заголовок на новый массив',
      ],
      explain:
        'Старый массив при этом не освобождается — его соберёт сборщик, когда на него не останется ссылок. Поэтому слайсы, срезанные раньше, продолжают видеть старые данные.',
    },
  ],
} satisfies Quiz
