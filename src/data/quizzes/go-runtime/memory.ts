import type { Quiz } from '../../../components/quiz/types.ts'

export default {
  questions: [
    {
      kind: 'single',
      q: 'По какому правилу компилятор решает, что объект должен жить в куче?',
      options: [
        'Если объект больше 64 байт',
        'Если указатель на объект может пережить функцию',
        'Если объект создан через `new` или `&T{}`',
        'Если объект содержит указатели',
      ],
      answer: 1,
      explain:
        'Это и есть escape-анализ. `&User{}` вполне может остаться на стеке, если указатель никуда не уходит. А вот возврат указателя, помещение в интерфейс, захват горутиной или размер, неизвестный при компиляции, отправляют объект в кучу.',
    },
    {
      kind: 'multi',
      q: 'Что из этого обычно заставляет значение убежать в кучу?',
      options: [
        'Функция возвращает `&u` на локальную переменную',
        'Значение передано в параметр типа `any` (например, в `fmt.Println`)',
        '`make([]byte, n)`, где `n` известно только в рантайме',
        'Локальная переменная `int`, используемая только внутри функции',
        'Горутина захватывает адрес локальной переменной',
      ],
      answer: [0, 1, 2, 4],
      explain:
        'Все четыре случая есть в лекции: указатель переживает функцию, значение попадает в интерфейс, размер неизвестен при компиляции, адрес захвачен горутиной. Проверить догадку можно через `go build -gcflags=-m`.',
    },
    {
      kind: 'truefalse',
      q: 'Когда стеку горутины не хватает места, рантайм выделяет вдвое больший кусок, копирует туда стек и правит указатели, которые вели внутрь старого стека.',
      answer: true,
      explain:
        'Дорастить стек на месте нельзя — рядом чужая память. Поэтому стек переезжает, а указатели на него рантайм находит по картам указателей в кадрах. По той же причине нельзя хранить указатели на память Go в C-структурах: о них рантайм не знает.',
    },
    {
      kind: 'single',
      q: 'Структура занимала 32 байта, в неё добавили поле, стало 33. Сколько займёт объект в куче?',
      options: ['33 байта', '40 байт', '48 байт', '64 байта'],
      answer: 2,
      explain:
        'Аллокатор выдаёт память только классами размеров: 8, 16, 24, 32, 48, 64… 33 байта округляются до 48 — треть слота пропадает, пока объект жив. Это внутренняя фрагментация, не утечка.',
    },
    {
      kind: 'order',
      q: 'Расставьте путь аллокации мелкого объекта в куче от самого быстрого шага к самому медленному.',
      items: [
        'Свободный слот в спане из `mcache` своего P',
        'Новый спан из `mcentral` — общего списка спанов этого класса',
        'Новые страницы из `mheap`',
        'Запрос памяти у операционной системы',
      ],
      explain:
        'Подавляющее большинство аллокаций заканчивается на первом шаге — без единой блокировки, потому что mcache принадлежит P. mcentral общий и под блокировкой, mheap — единственный владелец памяти процесса, а поход к ядру дороже всего на порядки.',
    },
    {
      kind: 'truefalse',
      q: 'Быстрый путь аллокации через `mcache` не берёт ни одной блокировки.',
      answer: true,
      explain:
        'mcache принадлежит процессору P, а Go-код без P не исполняется — значит, никто другой в этот кэш не заглянет. Горутины начинают мешать друг другу только у общей блокировки mcentral, когда кэши пустеют.',
    },
    {
      kind: 'multi',
      q: 'Какие объекты попадают в tiny-аллокатор (складываются вплотную в общий 16-байтовый блок)?',
      options: [
        'Меньше 16 байт',
        'Без указателей внутри',
        'Любые объекты меньше 32 КБ',
        'Только объекты на стеке',
      ],
      answer: [0, 1],
      explain:
        'Оба условия обязательны. Без указателей — потому что сборщику нужно знать, где лежат указатели, а в общем блоке эта информация была бы одна на всех. Подвох: блок живёт, пока жив хотя бы один объект из него.',
    },
    {
      kind: 'single',
      q: 'Программа отпустила много данных, сборщик отработал, а RSS процесса упал не сразу. Почему?',
      options: [
        'Это утечка памяти',
        'Освобождённые страницы сначала остаются в куче рантайма, ядру их потом отдаёт scavenger',
        'Сборщик Go никогда не возвращает память ОС',
        'RSS считает и стеки всех горутин',
      ],
      answer: 1,
      explain:
        'Рантайм не хочет ходить в ядро дважды за одной и той же памятью. Долго простоявшие страницы отдаёт фоновый scavenger, поэтому RSS падает с задержкой.',
    },
    {
      kind: 'single',
      q: 'Свободных страниц в куче хватает, а буфер на 12 страниц всё равно требует новой памяти у ОС. Что это?',
      options: [
        'Внешняя фрагментация: свободные страницы лежат не подряд, а сборщик не двигает объекты',
        'Внутренняя фрагментация классов размеров',
        'Ошибка пейсера',
        'Ограничение GOMEMLIMIT',
      ],
      answer: 0,
      explain:
        'Объекты больше 32 КБ требуют непрерывных страниц. Склеить разрозненные свободные страницы нельзя — сборщик неперемещающий. Опасный профиль — постоянные большие буферы разного размера; лечится переиспользованием буферов одного размера.',
    },
    {
      kind: 'order',
      q: 'В каком порядке стоит действовать, когда память стала проблемой?',
      items: [
        'Понять, что происходит: `gctrace` и профиль аллокаций',
        'Убрать лишние побеги в кучу по `-gcflags=-m`',
        'Переиспользовать вместо выделения: `sync.Pool`, предразмеренные слайсы',
        'Уменьшить объекты: поля, выравнивание, лишние указатели',
        'И только потом крутить GOGC и GOMEMLIMIT',
      ],
      explain:
        'Ручки сборщика меняют размен памяти на процессор, но не уменьшают число аллокаций. Дешевле всего — не выделять вовсе: это разгружает и аллокатор, и сборщик.',
    },
  ],
} satisfies Quiz
