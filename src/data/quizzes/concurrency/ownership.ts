import type { Quiz } from '../../../components/quiz/types.ts'

export default {
  questions: [
    {
      kind: 'multi',
      q: 'Что требует правило владельца от того, кто запустил горутину?',
      options: [
        'Горутина должна гарантированно завершаться при любом исходе',
        'Запустивший должен уметь её остановить',
        'Запустивший должен знать, когда она завершилась',
        'Горутина не должна обращаться к общей памяти',
        'Горутина должна перехватывать все паники',
      ],
      answer: [0, 1, 2],
      explain:
        'Три требования: завершиться, уметь остановить, знать о завершении. Горутина без любого из них — утечка или гонка, которые ещё не случились. Работа с общей памятью и recover — отдельные вопросы.',
    },
    {
      kind: 'single',
      q: 'Какой API лучше соответствует правилу владельца?',
      options: [
        '`func (s *Store) Warmup() { go s.loadAll() }`',
        '`func (s *Store) Warmup(ctx context.Context) error { return s.loadAll(ctx) }`',
        '`func (s *Store) Warmup() chan struct{}`, который никто не обязан читать',
        'Разницы нет, если loadAll быстрая',
      ],
      answer: 1,
      explain:
        'Синхронную функцию проще использовать правильно. Если вызывающему нужно фоном — он сам напишет `go s.Warmup(ctx)` и сам будет за эту горутину отвечать. Функция не должна запускать горутины, переживающие её вызов, если это не явная часть контракта.',
    },
    {
      kind: 'truefalse',
      q: 'Если горутина запаниковала, `recover` в функции, которая её запустила, перехватит панику.',
      answer: false,
      explain:
        'Паника раскручивает только стек своей горутины. Непойманная там паника роняет весь процесс. Горутины с чужим или ненадёжным кодом оборачивают в `defer`/`recover` внутри самой горутины — так делает `net/http` для каждого запроса.',
    },
    {
      kind: 'truefalse',
      q: 'Запись `pages[i] = ...` из разных горутин в разные элементы одного слайса — не гонка, мьютекс не нужен.',
      answer: true,
      explain:
        'Каждая горутина пишет в свой элемент — это разные ячейки памяти, гонки нет. А `wg.Wait()` гарантирует, что все записи видны после него. Это частный случай владения: у каждой ячейки один писатель.',
    },
    {
      kind: 'single',
      q: 'Чем `errgroup.WithContext` полезнее `WaitGroup` для загрузки нескольких страниц?',
      options: [
        'Он быстрее запускает горутины',
        'Первая ошибка отменяет контекст остальных, а `Wait` возвращает эту ошибку',
        'Он ограничивает число горутин по числу ядер',
        'Он перехватывает паники в горутинах',
      ],
      answer: 1,
      explain:
        'WaitGroup не знает об ошибках. errgroup возвращает первую ошибку и отменяет дочерний контекст — соседи видят отмену и выходят, не делая бесполезную работу. Переприсваивание `g, ctx := errgroup.WithContext(ctx)` нарочно прячет родительский контекст.',
    },
    {
      kind: 'truefalse',
      q: 'Отмена контекста прерывает горутину, даже если та занята долгим вычислением и в `ctx` не смотрит.',
      answer: false,
      explain:
        'Отмена кооперативная: `ctx.Done()` — лишь закрытый канал. Горутина должна сама смотреть в него — `select` на каждом блокирующем ожидании и проверка `ctx.Err()` в долгих циклах.',
    },
    {
      kind: 'single',
      q: 'В pprof 200 тысяч горутин в состоянии `chan send`. Где скорее всего утечка?',
      options: [
        'Отправитель ждёт получателя, который ушёл по таймауту, а канал без буфера',
        'Получатели не успевают разбирать канал',
        'Сборщик мусора не успевает собирать горутины',
        'Закончились потоки ОС',
      ],
      answer: 0,
      explain:
        'Горутина стоит на отправке — значит, принимать некому. Классика: обработчик ушёл по `ctx.Done()`, а горутина с результатом ждёт вечно. Лечится буфером на число ответов, которые могут остаться ненужными: `make(chan Result, 1)`.',
    },
    {
      kind: 'multi',
      q: 'Какими инструментами ловят утечки горутин?',
      options: [
        '`runtime.NumGoroutine()` в метриках — растёт и не падает',
        '`/debug/pprof/goroutine?debug=1` — стеки, сгруппированные по месту ожидания',
        '`go.uber.org/goleak` в тестах',
        '`go test -race`',
        'Детектор deadlock в рантайме',
      ],
      answer: [0, 1, 2],
      explain:
        'Детектор гонок ищет гонки, а не утечки. Рантайм ловит только ситуацию, когда спят все горутины сразу, — на сервере так не бывает. goleak ценнее всего: превращает утечку в красный тест, но работает, только если у компонентов есть способ остановиться.',
    },
    {
      kind: 'single',
      q: 'У фонового компонента есть `cancel` и канал `done`, который закрывается при выходе горутины. Зачем `Stop` ждёт `<-done` после `cancel()`?',
      options: [
        'Чтобы не было гонки на `cancel`',
        'Чтобы вызывающий знал, что горутина действительно завершилась, прежде чем закрыть ресурсы, которыми она пользуется',
        'Без этого `cancel` не сработает',
        'Чтобы тикер успел остановиться',
      ],
      answer: 1,
      explain:
        '`cancel` — второе требование (остановить), `done` — третье (знать, что остановилась). Без ожидания `Stop` вернулся бы, пока горутина ещё работает, и вызывающий закрыл бы базу у неё из-под ног.',
    },
  ],
} satisfies Quiz
