import type { Quiz } from '../../../components/quiz/types.ts'

export default {
  questions: [
    {
      kind: 'single',
      q: 'У обработчика 1 секунда на ответ, он делает три вызова, у каждого клиента свой таймаут 2 секунды. В чём проблема?',
      options: [
        'Никакой: каждый вызов ограничен',
        'Таймауты не складываются — обработчик может ответить через 6 секунд, когда клиент давно ушёл',
        'Таймауты конфликтуют и отменяют друг друга',
        'Второй вызов не начнётся, пока не истечёт таймаут первого',
      ],
      answer: 1,
      explain:
        'Правильно — один дедлайн запроса в контексте. Каждый следующий вызов получает остаток, а если клиент отключился, `r.Context()` отменится и остановит всю цепочку.',
    },
    {
      kind: 'truefalse',
      q: '`context.WithTimeout(ctx, 5*time.Second)` внутри контекста, у которого осталось 100 мс, даст дочернему контексту только эти 100 мс.',
      answer: true,
      explain:
        'Дедлайн выбирается самый ранний: дочерний контекст не может жить дольше родителя.',
    },
    {
      kind: 'single',
      q: 'Чем отличаются `context.DeadlineExceeded` и `context.Canceled` на практике?',
      options: [
        'Ничем — это синонимы',
        'Первое — мы не уложились в срок, второе — нас отменили (клиент ушёл, сосед по errgroup упал)',
        'Первое — ошибка сети, второе — ошибка базы',
        'Canceled возвращается только при панике',
      ],
      answer: 1,
      explain:
        'Причины разные, и в логах и метриках их стоит разделять: DeadlineExceeded — повод искать, кто медленный, Canceled — чаще всего нормальная отмена.',
    },
    {
      kind: 'single',
      q: 'В цикле с 10 000 событий в секунду стоит `case <-time.After(time.Minute)`. Что с памятью в модуле с `go 1.23` в `go.mod`?',
      options: [
        'Растёт: каждый таймер живёт минуту',
        'Не растёт: таймер без ссылок собирается сборщиком сразу',
        'Программа паникует',
        'Зависит только от версии компилятора, а не от `go.mod`',
      ],
      answer: 1,
      explain:
        'До 1.23 каждый `time.After` оставался в куче таймеров до срабатывания — 600 000 таймеров. С 1.23 неиспользуемые таймеры собираются сразу. Поведение определяется версией в `go.mod` модуля, а не компилятора.',
    },
    {
      kind: 'multi',
      q: 'Что верно про `time.Ticker`?',
      options: [
        'Он не копит пропущенные тики: после долгой работы придёт один тик, а не очередь',
        'Период отсчитывается от старта, а не от конца предыдущей работы',
        'Первый тик приходит сразу при создании',
        'Работа в той же горутине, что ждёт тиков, не накладывается сама на себя',
      ],
      answer: [0, 1, 3],
      explain:
        'Первый тик — через период. Если работу нужно сделать при старте, её вызывают до цикла. Если нужна пауза именно между выполнениями — таймер и `Reset` после работы.',
    },
    {
      kind: 'single',
      q: 'Десять реплик после выкатки одновременно запускают обновление кэша раз в 30 секунд и синхронно бьют в одну зависимость. Что помогает?',
      options: ['Уменьшить период', 'Случайный сдвиг — jitter — к периоду', 'Буферизованный канал тикера', 'Мьютекс вокруг обновления'],
      answer: 1,
      explain:
        'Небольшой случайный сдвиг размазывает нагрузку во времени. Мьютекс в памяти одного процесса другие реплики не видит.',
    },
    {
      kind: 'single',
      q: '`rate.NewLimiter(rate.Limit(50), 10)` — что это значит?',
      options: [
        '50 одновременных запросов, очередь на 10',
        'В среднем не больше 50 запросов в секунду, короткий всплеск до 10 без задержки',
        '10 запросов в секунду, всплеск до 50',
        '50 запросов за 10 секунд',
      ],
      answer: 1,
      explain:
        'Ведро с токенами: ёмкость 10, пополнение 50 в секунду, запрос забирает токен. Пул ограничивает, сколько задач одновременно, а лимитер — сколько в секунду.',
    },
    {
      kind: 'multi',
      q: 'Какие методы лимитера для чего подходят?',
      options: [
        '`Wait(ctx)` — для своих исходящих запросов: подождать токена',
        '`Allow()` — для чужих входящих: нет токена — ответить 429',
        '`Wait(ctx)` сразу вернёт ошибку, если по расчёту токен появится позже дедлайна',
        '`Allow()` блокируется до появления токена',
      ],
      answer: [0, 1, 2],
      explain:
        '`Allow` не блокирует: взял токен или `false`. `Reserve` бронирует токен и сообщает, сколько ждать, если решать нужно самому.',
    },
    {
      kind: 'truefalse',
      q: 'Лимитер в памяти процесса на 100 запросов в секунду ограничивает весь сервис из десяти реплик 100 запросами в секунду.',
      answer: false,
      explain:
        'Каждая реплика ограничивает только себя: внешний API получит до 1000. Общий лимит — деление квоты на число реплик (неточно при автомасштабировании) или счётчик во внешнем хранилище.',
    },
    {
      kind: 'single',
      q: 'Файл сохраняют пачкой изменений, пересобрать нужно один раз через 200 мс после последнего. Какой паттерн?',
      options: ['Throttle', 'Debounce', 'Ticker', 'Rate limit с Wait'],
      answer: 1,
      explain:
        'Debounce реагирует после затишья: каждое событие делает `Reset` таймера. Throttle — не чаще раза в период. Ловушка debounce: при непрерывном потоке он не сработает никогда, поэтому добавляют максимальное ожидание.',
    },
  ],
} satisfies Quiz
