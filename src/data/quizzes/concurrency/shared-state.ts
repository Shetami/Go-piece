import type { Quiz } from '../../../components/quiz/types.ts'

export default {
  questions: [
    {
      kind: 'multi',
      q: 'Что может случиться в программе с гонкой данных?',
      options: [
        'Потерянные увеличения счётчика',
        'Вечный цикл `for !done {}`: компилятор вправе не перечитывать переменную',
        'Чтение наполовину обновлённой строки или интерфейса',
        '`fatal error: concurrent map writes`, который не перехватывается `recover`',
        'Ничего страшного, если писатель только один',
      ],
      answer: [0, 1, 2, 3],
      explain:
        'Гонка — это не «иногда неправильное число», а неопределённое поведение. Одного писателя недостаточно: гонка — любое несинхронизированное чтение рядом с записью. Корень — отсутствие happens-before между записью и чтением.',
    },
    {
      kind: 'truefalse',
      q: '`go test -race` находит только те гонки, которые действительно случились на пройденных тестами путях.',
      answer: true,
      explain:
        'Детектор видит только то, что случилось в прогоне. Поэтому тесты под `-race` должны действительно гонять код конкурентно. Ложных срабатываний почти нет: сказал «гонка» — значит, она есть.',
    },
    {
      kind: 'single',
      q: 'У кэша потокобезопасные `Get` и `Set`. Код `if _, ok := c.Get(k); !ok { c.Set(k, load(k)) }` — что с ним?',
      options: [
        'Всё корректно: оба метода под мьютексом',
        'Логическая гонка: две горутины увидят промах и обе вызовут `load`. Детектор гонок её не заметит',
        'Гонка данных, детектор её поймает',
        'Deadlock: мьютекс берётся дважды',
      ],
      answer: 1,
      explain:
        'Мьютекс защищает каждую операцию, но не последовательность. Проверка и действие должны идти под одним захватом замка, а если `load` медленная — нужна склейка запросов (singleflight).',
    },
    {
      kind: 'multi',
      q: 'Какие правила обращения с мьютексом верны?',
      options: [
        'Мьютекс лежит рядом с данными, методы — на указателе',
        'В критической секции — только работа с данными, без сети и отправки в каналы',
        'Под замком нельзя вызывать чужой код: мьютекс в Go не реентерабелен',
        'Возвращённый из-под замка указатель на защищённый объект можно спокойно менять',
        'Копирование структуры с мьютексом безопасно — копируется и состояние замка',
      ],
      answer: [0, 1, 2],
      explain:
        'Копия структуры — копия мьютекса, и две копии защищают каждая своё (ловит `go vet`). А указатель, отданный наружу, выводит объект из-под защиты: мьютекс защищал map, а не сессии.',
    },
    {
      kind: 'truefalse',
      q: 'Для коротких секций чтения `RWMutex` всегда быстрее обычного `Mutex`.',
      answer: false,
      explain:
        'Каждый `RLock` — атомарная запись в общий счётчик читателей, и на многих ядрах эта строка кэша прыгает между ними. Для коротких секций это часто медленнее. Плюс ловушка: повторный `RLock` в той же горутине может зависнуть при ждущем писателе.',
    },
    {
      kind: 'single',
      q: 'Счётчики `requests` и `errors` — два `atomic.Int64`. Метрика читает их двумя `Load` и делит. Почему доля ошибок может выйти больше 100%?',
      options: [
        'Атомики на 32-битных платформах рвутся',
        'Между двумя `Load` другие горутины меняют счётчики: атомики защищают одну переменную, а не связь между ними',
        'Деление целых чисел округляет вверх',
        'Load не синхронизирует память',
      ],
      answer: 1,
      explain:
        'Каждый Load атомарен, их пара — нет. Если у данных есть инвариант между полями — нужен мьютекс или неизменяемый снимок.',
    },
    {
      kind: 'single',
      q: 'Таблица маршрутизации читается на каждом запросе, а меняется раз в минуту. Какой приём подходит лучше всего?',
      options: [
        'Мьютекс на каждое чтение',
        'Неизменяемый снимок: писатель строит новую таблицу и подменяет `atomic.Pointer`',
        '`sync.Map`',
        'Горутина-владелец, которой присылают запросы',
      ],
      answer: 1,
      explain:
        'Читатели не блокируются никогда и видят целостную версию — старую или новую. Условие одно: после `Store` таблицу никто не меняет. Если писателей несколько и они меняют текущую версию — CAS-цикл.',
    },
    {
      kind: 'single',
      q: 'Когда горутина-владелец состояния предпочтительнее мьютекса?',
      options: [
        'Когда нужна максимальная скорость простых операций над map',
        'Когда у состояния много разных событий — запросы, таймеры, остановка — и их удобно обрабатывать по очереди в одном `select`',
        'Всегда: каналы идиоматичнее мьютексов',
        'Когда данные — одна переменная',
      ],
      answer: 1,
      explain:
        'Владелец покупает ясность: о гонках не нужно думать вообще. Цена — две передачи по каналу на операцию; для простой map под нагрузкой мьютекс в разы быстрее.',
    },
    {
      kind: 'truefalse',
      q: 'Если инициализация в `sync.OnceValues` вернула ошибку, следующий вызов попробует инициализировать снова.',
      answer: false,
      explain:
        'Результат, включая ошибку, запоминается навсегда. Если нужна повторная попытка после неудачи, пишут свою обёртку с мьютексом и флагом, который ставится только при успехе.',
    },
    {
      kind: 'single',
      q: 'Горутины делят обычную map. С чего начать?',
      options: [
        'С `sync.Map` — она для этого и сделана',
        'С map под `sync.Mutex`; `sync.Map` или шардирование — после профиля',
        'С `RWMutex`',
        'С атомиков на каждый ключ',
      ],
      answer: 1,
      explain:
        'Документация `sync.Map` сама советует map с мьютексом в большинстве случаев. `sync.Map` выигрывает, когда ключи пишутся один раз и много читаются или горутины работают с разными ключами.',
    },
  ],
} satisfies Quiz
