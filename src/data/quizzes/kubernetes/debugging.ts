import type { Quiz } from '../../../components/quiz/types.ts'

export default {
  questions: [
    {
      kind: 'order',
      q: 'Расставьте шаги отладки проблемного пода от общего к частному.',
      items: [
        '`kubectl get pods -o wide` — статус, перезапуски, узел',
        '`kubectl describe pod` — подробности и события внизу',
        '`kubectl logs --previous` — что сказало приложение в упавшем запуске',
        '`kubectl get events --sort-by=.lastTimestamp` — что происходило в namespace',
      ],
      explain:
        'В девяти случаях из десяти причина написана в событиях открытым текстом. Но хранятся они недолго — по умолчанию час, — так что вчерашнюю аварию по ним не расследовать.',
    },
    {
      kind: 'single',
      q: 'Под в статусе `ImagePullBackOff`. Где причина?',
      options: [
        'В логах приложения',
        'В событиях: опечатка в имени образа, нет тега или нет доступа к реестру',
        'В readiness-пробе',
        'В лимитах памяти',
      ],
      answer: 1,
      explain:
        'Контейнер ещё не запускался — логов нет. Доступ к приватному реестру даёт `imagePullSecrets`. А `CreateContainerConfigError` — это отсутствующий ConfigMap, Secret или ключ в них.',
    },
    {
      kind: 'multi',
      q: 'Что означают коды выхода контейнера?',
      options: [
        '1 — приложение вышло с ошибкой само, например `log.Fatal`',
        '2 — паника Go',
        '137 — убит SIGKILL: память, liveness или не уложился в период остановки',
        '143 — OOMKilled',
      ],
      answer: [0, 1, 2],
      explain: '143 — это 128 + 15, завершение по SIGTERM. OOMKilled даёт 137: 128 + 9.',
    },
    {
      kind: 'single',
      q: 'Под `Running`, READY `0/1`, в событиях `Readiness probe failed: ... connection refused`. Что проверить первым?',
      options: [
        'Увеличить `timeoutSeconds`',
        'Адрес и порт, на которых слушает сервис: тот ли порт и не `localhost:8080` ли вместо `:8080`',
        'Добавить liveness',
        'Пересоздать под',
      ],
      answer: 1,
      explain:
        '`connection refused` — «никто не слушает», а не «медленно». Сервис на `127.0.0.1` работает изнутри контейнера, но kubelet стучится по IP пода. Liveness лишь зациклила бы перезапуски.',
    },
    {
      kind: 'single',
      q: 'Service: `port: 80`, `targetPort: 8080`. Клиент делает `GET http://api:8080` и получает таймаут. Почему?',
      options: [
        'NetworkPolicy блокирует 8080',
        'Обращаться надо к порту сервиса — 80; для `ClusterIP:8080` правил нет, пакет уходит в никуда',
        'kube-proxy сломан',
        'DNS возвращает неверный адрес',
      ],
      answer: 1,
      explain:
        '`port` — на чём слушает сервис, `targetPort` — куда он перенаправит в поде. Путаницы меньше, если сделать их одинаковыми или назвать порт по имени (`targetPort: http`).',
    },
    {
      kind: 'order',
      q: 'Поды работают, а запросы не доходят. Расставьте проверки по цепочке.',
      items: [
        'Поды готовы?',
        'EndpointSlice сервиса не пустой?',
        'Селектор совпадает с метками, порты сходятся?',
        'Изнутри кластера сервис отвечает по имени (netshoot)?',
        'Снаружи — Ingress/Gateway: хост, путь, класс, TLS',
      ],
      explain:
        'Отвечает напрямую, но не через сервис — порты или селектор. Отвечает из своего namespace, но не из чужого — NetworkPolicy. Изнутри кластера, но не снаружи — Ingress.',
    },
    {
      kind: 'single',
      q: 'Как отладить контейнер из distroless-образа, где нет оболочки?',
      options: [
        '`kubectl exec -it <под> -- sh`',
        'Эфемерный контейнер: `kubectl debug -it <под> --image=nicolaka/netshoot --target=api`',
        'Пересобрать образ с bash',
        'Никак',
      ],
      answer: 1,
      explain:
        'Эфемерный контейнер подселяется в работающий под без перезапуска, видит его сеть и процессы, а через `/proc/<pid>/root` — и файловую систему.',
    },
    {
      kind: 'truefalse',
      q: 'pprof для кластера удобнее всего снимать через `kubectl port-forward` на Service.',
      answer: false,
      explain:
        'Через Service вы попадёте в случайный под, а нужен именно тот, что ведёт себя странно. `port-forward pod/<имя> 6060:6060`, и pprof — на отдельном порту, не наружу.',
    },
    {
      kind: 'single',
      q: 'Когда `kubectl delete pod --force --grace-period=0` особенно опасен?',
      options: [
        'Для подов Deployment',
        'Для подов StatefulSet на недоступном узле: процесс может продолжать работать, и замена запишет в те же данные',
        'Для Job',
        'Никогда не опасен',
      ],
      answer: 1,
      explain:
        'Команда удаляет объект из API, не дожидаясь подтверждения остановки. Прежде чем так делать, разберитесь, почему под висит: долгая остановка, недоступный узел или finalizer.',
    },
  ],
} satisfies Quiz
