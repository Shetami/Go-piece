import type { Quiz } from '../../../components/quiz/types.ts'

export default {
  questions: [
    {
      kind: 'single',
      q: 'Что на самом деле говорит манифест Deployment с `replicas: 3`?',
      options: [
        '«Запусти три копии» — одноразовая команда',
        '«Должно работать три копии» — кластер будет поддерживать это состояние всегда',
        '«Запусти не больше трёх копий»',
        '«Запусти три копии на трёх разных узлах»',
      ],
      answer: 1,
      explain:
        'Вы описываете желаемое состояние, а не переход. Упал процесс — копий стало две — кластер снова сделает три. Поломка для Kubernetes — просто ещё одно расхождение «есть» и «должно быть».',
    },
    {
      kind: 'multi',
      q: 'Какие свойства у цикла согласования контроллера?',
      options: [
        'Он реагирует на состояние, а не на события — пропущенное событие ничего не ломает',
        'Он делает маленький шаг и повторяет',
        'Контроллеры не знают друг о друге и общаются только через объекты',
        'Контроллер вызывает следующий контроллер напрямую',
      ],
      answer: [0, 1, 2],
      explain:
        'Контроллер Deployment не вызывает контроллер ReplicaSet — он записывает объект, а тот видит его через API-сервер. Поэтому остановившаяся работа сама продолжается, когда компонент поднимается.',
    },
    {
      kind: 'single',
      q: 'С кем напрямую разговаривает etcd?',
      options: ['Со всеми компонентами кластера', 'Только с API-сервером', 'С kubelet на каждом узле', 'С kubectl'],
      answer: 1,
      explain:
        'API-сервер — единственная дверь в кластер: он проверяет права и формат, хранит объекты в etcd и рассылает изменения подписчикам. Все остальные говорят только с ним.',
    },
    {
      kind: 'order',
      q: 'Расставьте, что происходит после `kubectl apply` с Deployment.',
      items: [
        'API-сервер проверяет и сохраняет Deployment в etcd',
        'Контроллер Deployment создаёт ReplicaSet',
        'Контроллер ReplicaSet создаёт объекты Pod без узла',
        'Планировщик выбирает каждому поду узел и пишет `spec.nodeName`',
        'kubelet узла запускает контейнеры и пишет статус `Running`',
      ],
      explain:
        '`kubectl apply` заканчивается на первом шаге — подов он не ждёт. Потом, когда под пройдёт readiness, контроллер EndpointSlice добавит его адрес в сервис.',
    },
    {
      kind: 'single',
      q: 'Планировщик упал, и вы удалили под. Что будет?',
      options: [
        'Под не удалится',
        'Под удалится, ReplicaSet создаст новый, но тот будет в Pending, пока планировщик не вернётся',
        'ReplicaSet не создаст новый под без планировщика',
        'Кластер откажется удалять под',
      ],
      answer: 1,
      explain:
        'Удаление и создание объекта планировщику не нужны. Нужен он только чтобы выбрать узел. Вернётся — увидит под без `nodeName` и назначит.',
    },
    {
      kind: 'truefalse',
      q: 'Если API-сервер недоступен, работающие поды продолжают работать, а упавший контейнер kubelet перезапустит.',
      answer: true,
      explain:
        'Кластер «замерзает» в последнем известном состоянии: сеть работает, контейнеры тоже. Не работает всё, что требует решения: новые поды, выкатка, переезд реплик, автомасштабирование.',
    },
    {
      kind: 'single',
      q: 'Кто пишет поля `spec` и `status` объекта?',
      options: [
        'Оба пишете вы',
        '`spec` — вы (что должно быть), `status` — контроллер (что есть)',
        '`spec` — контроллер, `status` — вы',
        'Оба пишет API-сервер',
      ],
      answer: 1,
      explain:
        'Расхождение `spec` и `status` значит, что контроллер ещё работает или не может выполнить просьбу. Причина обычно видна в событиях: `kubectl describe`.',
    },
    {
      kind: 'truefalse',
      q: 'Service находит свои поды по ссылке на Deployment.',
      answer: false,
      explain:
        'Связи — через метки и селекторы. Отсюда самая частая ошибка новичка: опечатка в метке, и сервис «не видит» поды, хотя все объекты на месте.',
    },
    {
      kind: 'single',
      q: 'Как контроллеры в `client-go` узнают об изменениях объектов?',
      options: [
        'Опрашивают API раз в секунду',
        'Через watch — долгий запрос, по которому приходит каждое изменение; поверх него информеры с локальным кэшем',
        'Читают etcd напрямую',
        'Получают вебхуки от kubelet',
      ],
      answer: 1,
      explain:
        'Опрос тысяч объектов был бы слишком дорог. Для своего оператора удобнее `controller-runtime` или kubebuilder: остаётся написать одну функцию `Reconcile`.',
    },
  ],
} satisfies Quiz
