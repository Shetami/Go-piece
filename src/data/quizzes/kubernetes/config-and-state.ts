import type { Quiz } from '../../../components/quiz/types.ts'

export default {
  questions: [
    {
      kind: 'truefalse',
      q: 'Если поменять значение в ConfigMap, работающие поды, получающие его переменной окружения, увидят новое значение.',
      answer: false,
      explain:
        'Переменные окружения задаются при запуске процесса, а шаблон пода не изменился — перезапуска нет. Новое значение увидят только новые поды, и конфигурация кластера тихо разъедется.',
    },
    {
      kind: 'single',
      q: 'Как надёжнее всего применять изменения ConfigMap?',
      options: [
        'Удалять поды руками по одному',
        'Положить хэш конфигурации в аннотацию шаблона пода: изменилась конфигурация — Deployment плавно заменил поды',
        'Перезапускать узлы',
        'Никак — ConfigMap применяется сам',
      ],
      answer: 1,
      explain:
        'Конфигурация меняется так же, как код: плавно и с остановкой на первом сломанном поде. Helm и Kustomize умеют это из коробки. Перечитывание файлов на лету — для часто меняющегося, вроде флагов функций.',
    },
    {
      kind: 'truefalse',
      q: 'Файл ConfigMap, смонтированный через `subPath`, обновляется так же, как обычный том.',
      answer: false,
      explain: 'С `subPath` файл не обновляется вовсе. Обычный том kubelet обновляет атомарно через симлинк, с задержкой до минуты.',
    },
    {
      kind: 'multi',
      q: 'Что на самом деле защищает Secret?',
      options: [
        'Права доступа через RBAC — включая право создавать поды в namespace',
        'Шифрование etcd',
        'Хранение вне git: External Secrets, Sealed Secrets, SOPS',
        'Кодирование значений в base64',
      ],
      answer: [0, 1, 2],
      explain:
        'base64 раскодируется за миллисекунду — он нужен для бинарных данных, а не для секретности. Под может смонтировать любой секрет своего namespace, поэтому право создавать поды равно доступу к паролям.',
    },
    {
      kind: 'single',
      q: 'Почему секреты лучше подавать файлами, а не переменными окружения?',
      options: [
        'Файлы быстрее читаются',
        'Переменные попадают в дампы и логи «всего окружения» и не обновляются при ротации; файл лежит в tmpfs и обновляется сам',
        'Переменные ограничены 1 КБ',
        'Kubernetes запрещает секреты в переменных',
      ],
      answer: 1,
      explain: 'Секрет, смонтированный файлом, живёт в памяти, а не на диске узла, и подменяется при ротации без перезапуска.',
    },
    {
      kind: 'single',
      q: 'Deployment на три реплики хочет общий облачный блочный диск через PVC `ReadWriteOnce`. Что будет?',
      options: [
        'Все три пода получат диск',
        'Диск подключится к одному узлу: поды на других узлах его не получат',
        'Kubernetes создаст три диска',
        'Диск станет доступен только на чтение',
      ],
      answer: 1,
      explain:
        'Облачные блочные диски обычно подключаются к одному узлу и живут в одной зоне. Для общего доступа — `ReadWriteMany` на сетевой ФС, но для Go-сервиса почти всегда правильнее объектное хранилище.',
    },
    {
      kind: 'multi',
      q: 'Что гарантирует StatefulSet?',
      options: [
        'Постоянные имена подов: `db-0`, `db-1`…',
        'У каждого пода свой PVC, и пересозданный под получает именно его',
        'Постоянные DNS-адреса через headless Service',
        'Настроенную репликацию и автоматическое переключение ведущего',
      ],
      answer: [0, 1, 2],
      explain:
        'StatefulSet не делает из приложения кластер: репликацию и переключение делают операторы (CloudNativePG, Strimzi) или сама база.',
    },
    {
      kind: 'single',
      q: 'Узел с подом `db-0` пропал из сети. Что сделает StatefulSet?',
      options: [
        'Сразу создаст `db-0` на другом узле',
        'Будет ждать, пока не убедится, что старый `db-0` мёртв: двух подов с одним именем быть не должно',
        'Создаст `db-3`',
        'Удалит PVC',
      ],
      answer: 1,
      explain:
        'Недоступный узел — не мёртвый. Второй `db-0` с тем же диском мог бы писать в одни данные с первым. Deployment так не осторожничает — его поды взаимозаменяемы.',
    },
    {
      kind: 'multi',
      q: 'Что важно для периодической работы в Kubernetes?',
      options: [
        'CronJob с `concurrencyPolicy: Forbid`, чтобы запуски не накладывались',
        'Идемпотентность: Job не обещает ровно один запуск',
        '`time.Ticker` в одной из реплик сервиса — самый простой вариант',
        '`activeDeadlineSeconds` и `ttlSecondsAfterFinished` для ограничения и уборки',
      ],
      answer: [0, 1, 3],
      explain:
        'Тикер в сервисе с тремя репликами выполнит задачу трижды. По умолчанию `concurrencyPolicy` — `Allow`, и долгий отчёт начнёт накладываться на следующий.',
    },
  ],
} satisfies Quiz
