import type { Quiz } from '../../../components/quiz/types.ts'

export default {
  questions: [
    {
      kind: 'single',
      q: 'Бинарник собран в образе `golang:1.25` командой `go build` без переменных окружения и скопирован в `distroless/static`. Сервис использует `net/http`. Что будет при запуске?',
      options: [
        'Всё заработает',
        '`exec /app: no such file or directory`: бинарник динамический, и в образе нет загрузчика и libc',
        'Сервис стартует, но не сможет резолвить имена',
        'Ошибка прав: distroless запрещает запуск бинарников',
      ],
      answer: 1,
      explain:
        'В образе `golang` есть `gcc`, поэтому по умолчанию `CGO_ENABLED=1`, и пакет `net` линкуется с libc. Ядро не находит загрузчик и возвращает «файл не найден». Лечение — `CGO_ENABLED=0`.',
    },
    {
      kind: 'single',
      q: 'Почему в Dockerfile сначала копируют `go.mod` и `go.sum`, делают `go mod download`, а уже потом `COPY . .`?',
      options: [
        'Так требует Go',
        'Слой со скачанными модулями берётся из кэша, пока не изменились зависимости, — правка кода не заставляет качать модули заново',
        'Чтобы итоговый образ был меньше',
        'Чтобы исключить `.git` из образа',
      ],
      answer: 1,
      explain:
        'Слой пересобирается, если изменился он или любой слой под ним. Код меняется в каждом коммите, зависимости — редко, поэтому редко меняющееся кладут ниже.',
    },
    {
      kind: 'truefalse',
      q: 'Флаги `-ldflags="-s -w"` ломают стеки паники и профили pprof.',
      answer: false,
      explain:
        'Они убирают таблицу символов и DWARF. Стекам и pprof нужна отдельная таблица `pclntab`, которую эти флаги не трогают. Перестаёт работать только отладчик на этом бинарнике.',
    },
    {
      kind: 'truefalse',
      q: 'Образ по тегу `api:1.4.2` завтра может оказаться другим образом, а по дайджесту `api@sha256:…` — нет.',
      answer: true,
      explain:
        'Тег — изменяемая ссылка, дайджест — хеш содержимого. Поэтому выкатывают и продвигают по дайджесту, а реестр настраивают так, чтобы теги не перезаписывались.',
    },
    {
      kind: 'multi',
      q: 'Почему пересборка того же коммита тем же Dockerfile обычно даёт другой дайджест?',
      options: [
        'В слоях записано время создания и модификации файлов',
        'Базовый образ по тегу `golang:1.25` мог обновиться между сборками',
        'Компилятор Go каждый раз добавляет случайные байты',
        'Метаданные и история сборки отличаются',
      ],
      answer: [0, 1, 3],
      explain:
        'Сам компилятор Go детерминирован. Отличия дают метаданные образа и изменившиеся базовые образы. Воспроизводимость достигается базами по дайджесту, `SOURCE_DATE_EPOCH` и `-trimpath`.',
    },
    {
      kind: 'single',
      q: 'В CI используется `RUN --mount=type=cache,target=/root/.cache/go-build` и `cache-to: type=gha,mode=max`. Сохранится ли кэш сборки Go между запусками на эфемерных раннерах?',
      options: [
        'Да: mode=max сохраняет всё',
        'Нет: cache mount живёт внутри сборщика и не экспортируется вместе с кэшем слоёв',
        'Да, если указать тот же ключ',
        'Нет, потому что Go не поддерживает кэш в Docker',
      ],
      answer: 1,
      explain:
        '`cache-to` экспортирует слои, а cache mount — не слой. На эфемерной машине он каждый раз пустой. Альтернатива — собирать бинарник вне Docker с кэшем `setup-go` и только копировать его в образ.',
    },
    {
      kind: 'single',
      q: 'Как быстро собрать образ Go-сервиса под `linux/arm64` на машине amd64?',
      options: [
        'Через эмуляцию QEMU — других способов нет',
        'Запустить этап сборки на родной платформе (`--platform=$BUILDPLATFORM`) и кросс-компилировать через `GOARCH=$TARGETARCH`',
        'Собрать на amd64 — бинарник запустится везде',
        'Использовать `scratch` вместо distroless',
      ],
      answer: 1,
      explain:
        'Go кросс-компилирует без эмуляции. Под QEMU компиляция в разы медленнее; эмулировать нужно только тривиальное копирование бинарника в итоговый образ. С cgo так просто не выйдет.',
    },
    {
      kind: 'multi',
      q: 'Что стоит сделать, чтобы на инциденте сразу понять, какой код работает?',
      options: [
        'Вшить версию и коммит в бинарник (`-X` или `debug.ReadBuildInfo`)',
        'Отдавать их эндпоинтом `/version` и писать в лог при старте',
        'Экспортировать метрику `build_info` с метками версии и коммита',
        'Выкатывать тег `latest`, чтобы всегда была последняя версия',
      ],
      answer: [0, 1, 2],
      explain:
        '`latest` как раз не говорит, что работает. Версия в бинарнике, `/version` и `build_info` на дашборде отвечают сразу, а смена значения метрики — это отметка выкатки.',
    },
  ],
} satisfies Quiz
