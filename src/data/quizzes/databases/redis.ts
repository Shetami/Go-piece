import type { Quiz } from '../../../components/quiz/types.ts'

export default {
  questions: [
    {
      kind: 'single',
      q: 'Почему `INCR` в Redis не теряет обновления, даже если его одновременно вызывают сто реплик сервиса?',
      options: [
        'Redis берёт блокировку на каждый ключ',
        'Команды исполняет один поток, строго по одной — две команды не пересекаются',
        'Каждый `INCR` проходит через транзакцию с уровнем Serializable',
        'Клиент go-redis сериализует вызовы',
      ],
      answer: 1,
      explain:
        'Данные трогает один поток, поэтому каждая команда атомарна без всяких блокировок. Та же причина делает медленную команду проблемой для всех клиентов сразу.',
    },
    {
      kind: 'multi',
      q: 'Какие действия на проде могут остановить Redis для всех клиентов на заметное время?',
      options: [
        '`KEYS *` на десятках миллионов ключей',
        '`DEL` хэша на миллион полей',
        '`SCAN` с курсором и `COUNT 100`',
        'Lua-скрипт, который в цикле обходит большой sorted set',
        '`UNLINK` большого ключа',
      ],
      answer: [0, 1, 3],
      explain:
        'Всё, что делает O(N) работы в единственном потоке, задерживает остальных. `SCAN` возвращает ключи порциями и пропускает других между ними, а `UNLINK` освобождает память в фоновом потоке.',
    },
    {
      kind: 'single',
      q: 'Круг по сети до Redis — 0,5 мс. Сколько примерно займут 1000 `GET` подряд в цикле?',
      options: ['Около 1 мс', 'Около 50 мс', 'Около 500 мс', 'Около 5 секунд'],
      answer: 2,
      explain:
        'Тысяча кругов по 0,5 мс — полсекунды, почти всё время — ожидание сети. Конвейер или `MGET` превращают это в один круг.',
    },
    {
      kind: 'truefalse',
      q: 'Если одна команда внутри `MULTI`/`EXEC` упала при исполнении, Redis откатывает остальные команды транзакции.',
      answer: false,
      explain:
        'Отката в Redis нет: остальные команды выполнятся. Транзакция отменяется целиком, только если команда была ошибочной ещё при постановке в очередь. `MULTI` даёт изоляцию, но не «всё или ничего».',
    },
    {
      kind: 'single',
      q: 'Redis — кэш с `maxmemory` и политикой `volatile-lru`, значения кладут через `SET` без TTL. Что будет, когда память заполнится?',
      options: [
        'Вытеснятся давно не использованные ключи',
        'Запись начнёт получать ошибку OOM: ключей с TTL нет, вытеснять нечего',
        'Redis вытеснит случайные ключи',
        'Redis начнёт сбрасывать ключи на диск',
      ],
      answer: 1,
      explain:
        '`volatile-*` выбирает только среди ключей с TTL. Для чистого кэша берут `allkeys-lru` или `allkeys-lfu`.',
    },
    {
      kind: 'single',
      q: 'AOF включён с `appendfsync everysec`, RDB снимается раз в 5 минут. Процесс Redis упал. Сколько подтверждённых записей может пропасть?',
      options: [
        'Ничего: «OK» означает, что запись на диске',
        'До одной-двух секунд записей',
        'До пяти минут записей',
        'Всё, что было в памяти',
      ],
      answer: 1,
      explain:
        'При загрузке Redis берёт AOF как более полный источник. Журнал сбрасывается на диск раз в секунду, поэтому теряется последняя секунда-две. Без AOF потерялось бы всё после последнего снимка.',
    },
    {
      kind: 'truefalse',
      q: 'Для RDB-снимка Redis делает `fork`, и при активной записи во время снимка памяти может понадобиться почти вдвое больше, чем занимают данные.',
      answer: true,
      explain:
        'Родитель и дочерний процесс делят память через copy-on-write, и каждая изменённая родителем страница копируется. Отсюда запас памяти под `maxmemory`, отключённые THP и совет держать несколько небольших инстансов вместо одного огромного.',
    },
    {
      kind: 'order',
      q: 'Расставьте шаги, которыми ClusterClient выполняет `GET user:42` по устаревшей таблице слотов.',
      items: [
        'Посчитать слот: CRC16(`user:42`) mod 16384',
        'По своей таблице отправить команду узлу, который раньше владел слотом',
        'Получить `-MOVED` с адресом нового владельца',
        'Обновить таблицу слотов и повторить команду на правильном узле',
      ],
      explain:
        '`MOVED` — постоянный переезд: клиент запоминает новый адрес. `ASK` во время миграции слота — одноразовое перенаправление без изменения таблицы.',
    },
    {
      kind: 'single',
      q: 'В Redis Cluster скрипт получает ключи `user:7` и `cart:7` и падает с `CROSSSLOT`. Как это исправить, не отказываясь от скрипта?',
      options: [
        'Выполнить скрипт через `MULTI`',
        'Назвать ключи `{user:7}:profile` и `{user:7}:cart`, чтобы слот считался по общей части в скобках',
        'Включить `cluster-allow-cross-slot yes`',
        'Отправить скрипт на все ведущие через `ForEachMaster`',
      ],
      answer: 1,
      explain:
        'Команды над несколькими ключами работают только внутри одного слота. Хэш-тег кладёт связанные ключи вместе, но слишком широкий тег соберёт на одном узле горячую партицию.',
    },
    {
      kind: 'single',
      q: 'Воркер взял блокировку `SET lock <токен> NX PX 10000`, перед записью в базу проверил, что в `lock` всё ещё его токен, и записал. Может ли запись сделать и второй воркер одновременно?',
      options: [
        'Нет: проверка токена перед записью это исключает',
        'Да: между проверкой и записью может случиться пауза дольше TTL, и блокировку возьмёт другой',
        'Нет, если освобождать блокировку Lua-скриптом',
        'Только если Redis работает в кластере',
      ],
      answer: 1,
      explain:
        'Блокировка с TTL — аренда, и пауза GC или повисший вызов переживает её. Для корректности нужна проверка на стороне ресурса — токен ограждения, ограничение в базе или идемпотентность.',
    },
  ],
} satisfies Quiz
