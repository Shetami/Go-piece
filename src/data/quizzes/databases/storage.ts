import type { Quiz } from '../../../components/quiz/types.ts'

export default {
  questions: [
    {
      kind: 'single',
      q: 'За что на самом деле «платит» запрос в реляционной базе?',
      options: ['За число прочитанных строк', 'За число прочитанных страниц', 'За число колонок в SELECT', 'За длину SQL-текста'],
      answer: 1,
      explain:
        'База читает и пишет страницами — в PostgreSQL по 8 КБ. Одна нужная строка и сто её соседей по странице стоят одно чтение, две строки на разных страницах — два.',
    },
    {
      kind: 'truefalse',
      q: 'В PostgreSQL строки таблицы хранятся отсортированными по первичному ключу.',
      answer: false,
      explain:
        'Таблица — куча страниц: строки лежат там, куда нашлось место. `ORDER BY id` без индекса — это сортировка всей таблицы. Адрес строки — `ctid`: номер страницы и указателя в ней.',
    },
    {
      kind: 'order',
      q: 'Что происходит при UPDATE строки, пока всё идёт штатно?',
      items: [
        'Изменение описывается в журнале WAL',
        'Страница меняется в буферном кэше и помечается грязной',
        'Коммит ждёт сброса на диск только журнала',
        'Грязная страница уходит на диск позже — фоном или на контрольной точке',
      ],
      explain:
        'Долговечность обеспечивает журнал, а страницы пишутся, когда удобно, пачками. Если сервер упадёт раньше, изменения восстановятся из WAL.',
    },
    {
      kind: 'single',
      q: 'Почему поиск по ключу в B-дереве на миллионы строк стоит всего одно-два чтения с диска?',
      options: [
        'B-дерево целиком хранится в памяти',
        'Узел — это страница с сотнями ключей, дерево получается низким, а верхние уровни почти всегда в кэше',
        'Индекс хранит сами строки, и в таблицу ходить не нужно',
        'Поиск использует хеш ключа',
      ],
      answer: 1,
      explain:
        'При трёхстах ключах на страницу три уровня покрывают 27 миллионов строк. Корень и верхние уровни горячие и лежат в кэше.',
    },
    {
      kind: 'multi',
      q: 'Индекс по `(user_id, created_at)`. Какие запросы смогут им эффективно воспользоваться?',
      options: [
        '`WHERE user_id = 42`',
        '`WHERE user_id = 42 ORDER BY created_at DESC LIMIT 10`',
        '`WHERE created_at > now() - interval \'1 day\'`',
        '`WHERE user_id = 42 AND created_at > \'2026-01-01\'`',
      ],
      answer: [0, 1, 3],
      explain:
        'Составной индекс работает слева направо: упорядочен по `user_id`, внутри — по дате. Условие только на вторую колонку разбросано по всему дереву — спуститься к нему нельзя.',
    },
    {
      kind: 'truefalse',
      q: 'Неиспользуемый индекс не только занимает место, но и замедляет каждую вставку в таблицу.',
      answer: true,
      explain:
        'Каждая вставка в таблицу — это вставка в каждый её индекс. Неиспользуемый индекс замедляет запись. Искать такие — `pg_stat_user_indexes` с `idx_scan = 0`.',
    },
    {
      kind: 'single',
      q: 'Запросу нужен 1% строк, разбросанных по большой таблице. Настройки PostgreSQL по умолчанию. Что скорее выберет планировщик вместо обычного чтения по индексу?',
      options: [
        'Index Scan — 1% это мало',
        'Seq Scan или Bitmap Heap Scan: с `random_page_cost = 4` случайные чтения по индексу оцениваются дороже',
        'Всегда Index Only Scan',
        'Nested Loop',
      ],
      answer: 1,
      explain:
        'Каждая строка по индексу — отдельное случайное чтение. С настройками по умолчанию индекс проигрывает уже на доле около 1/130. На SSD `random_page_cost` обычно снижают до 1.1 — и тот же запрос получает индексный план.',
    },
    {
      kind: 'single',
      q: 'На что смотреть в `EXPLAIN ANALYZE` в первую очередь?',
      options: [
        'На `cost` — чем меньше, тем лучше',
        'На расхождение оценки `rows` и `actual rows`: ошибка в десятки раз почти всегда объясняет плохой план',
        'На `width`',
        'На число `loops`',
      ],
      answer: 1,
      explain:
        'Планировщик не знает число строк, а угадывает по статистике `ANALYZE`. Второе, что стоит смотреть, — `Buffers`: сколько страниц из кэша и сколько с диска. Это честнее времени.',
    },
    {
      kind: 'multi',
      q: 'Чем LSM-дерево отличается от B-дерева?',
      options: [
        'Не меняет данные на диске на месте — пишет новые отсортированные файлы',
        'Запись дешевле, чтение ключа дороже: он может быть в памяти или в любом из файлов',
        'Удаление — это запись-«надгробие», место освобождается после компакции',
        'Не требует никакой фоновой работы',
      ],
      answer: [0, 1, 2],
      explain:
        'Компакция постоянно переписывает данные фоном и отнимает диск и процессор. Фильтры Блума ускоряют чтение, говоря «этого ключа в файле точно нет». B-дерево бережёт чтение, LSM — запись.',
    },
    {
      kind: 'truefalse',
      q: 'Для запроса «средний чек по месяцам за три года» колоночное хранилище обычно выгоднее строкового.',
      answer: true,
      explain:
        'Запросу нужны две колонки из тридцати. Строковое хранилище читает все, колоночное — только нужные, и однотипные значения подряд хорошо сжимаются. Поэтому аналитику часто уводят в ClickHouse, а транзакционная нагрузка остаётся в PostgreSQL.',
    },
  ],
} satisfies Quiz
