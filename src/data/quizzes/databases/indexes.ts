import type { Quiz } from '../../../components/quiz/types.ts'

export default {
  questions: [
    {
      kind: 'single',
      q: 'В плане Index Scan есть `Filter: (status = \'new\')` и `Rows Removed by Filter: 1840`. Что это значит?',
      options: [
        'Индекс работает идеально',
        'Индекс нашёл много лишних строк: `status` проверяется после чтения, а не сужает поиск',
        'Индекс не используется',
        'Статистика устарела',
      ],
      answer: 1,
      explain:
        'Index Cond — условия, по которым индекс ищет. Filter — то, что проверяется после чтения, и каждая отброшенная строка прочитана зря. Хороший индекс под запрос — почти всё в Index Cond.',
    },
    {
      kind: 'single',
      q: 'Частый запрос: `WHERE status = \'new\' AND created_at > $1 ORDER BY created_at LIMIT 50`. Какой индекс ему нужен?',
      options: ['`(created_at, status)`', '`(status, created_at)`', 'Два отдельных — по `status` и по `created_at`', 'Hash по `status`'],
      answer: 1,
      explain:
        'Сначала колонки с равенством, потом одна колонка с диапазоном или сортировкой. Равенство выбирает непрерывный кусок, внутри которого записи уже по дате — 50 подряд и без сортировки. «Селективную колонку первой» — неверный совет: решает форма запроса.',
    },
    {
      kind: 'truefalse',
      q: 'При наличии индекса `(status, created_at)` отдельный индекс по `status` лишний.',
      answer: true,
      explain:
        'Составной индекс обслуживает и запросы по своему началу. Наоборот не работает: индекс по `status` не заменит `(status, created_at)` для диапазона по дате.',
    },
    {
      kind: 'single',
      q: 'Таблицу только что залили миллионом строк. В плане Index Only Scan с `Heap Fetches: 1000000`. Почему?',
      options: [
        'Индекс повреждён',
        'Карта видимости пуста — VACUUM ещё не прошёл, и видимость приходится проверять по таблице',
        'Не хватает колонок в INCLUDE',
        'Так работает любой Index Only Scan',
      ],
      answer: 1,
      explain:
        'Индекс не знает о видимости версий. Биты «всё видно всем» ставит VACUUM. После заливки — `VACUUM (ANALYZE)`: заполнит карту видимости и соберёт статистику.',
    },
    {
      kind: 'single',
      q: 'Частичный индекс `WHERE status = \'pending\'` работает в psql, но не подхватывается из Go через pgx. Вероятная причина?',
      options: [
        'pgx не умеет работать с частичными индексами',
        'Запрос с `WHERE status = $1` после нескольких выполнений получает общий план, не знающий значения параметра',
        'Нужен REINDEX',
        'В Go другой часовой пояс',
      ],
      answer: 1,
      explain:
        'Планировщик должен доказать, что условие запроса влечёт условие индекса. Общий план подготовленного запроса значения `$1` не знает. Лечится литералом в условии или `plan_cache_mode`.',
    },
    {
      kind: 'multi',
      q: 'Что верно про индекс по выражению, например `(lower(email))`?',
      options: [
        'Выражение в запросе должно совпадать с выражением индекса буквально',
        'Функция должна быть `IMMUTABLE`',
        '`created_at::date` у `timestamptz` индексировать нельзя: результат зависит от часового пояса сессии',
        'Он ускоряет и запросы по `email` без `lower`',
      ],
      answer: [0, 1, 2],
      explain:
        'Индекс хранит результат выражения, а не значение колонки, поэтому условие на саму колонку он не обслужит.',
    },
    {
      kind: 'single',
      q: 'Как запретить две брони одной переговорки на пересекающееся время так, чтобы это работало при любом уровне изоляции?',
      options: [
        'UNIQUE по `(room_id, start)`',
        'Ограничение исключения `EXCLUDE USING gist (room_id WITH =, during WITH &&)`',
        'Проверка в приложении перед INSERT',
        'BRIN-индекс по времени',
      ],
      answer: 1,
      explain:
        'GiST умеет «пересекается». Ограничение в схеме — самое надёжное лекарство от перекоса записи: проверка в приложении на READ COMMITTED пропустит две параллельные брони.',
    },
    {
      kind: 'single',
      q: 'Журнал событий в терабайт, строки дописываются по времени, запросы «за вчера». Какой индекс занимает мегабайты и хорошо подходит?',
      options: ['B-дерево', 'GIN', 'BRIN', 'Hash'],
      answer: 2,
      explain:
        'BRIN хранит минимум и максимум на группу страниц. Работает, только если данные лежат по порядку — проверять по `pg_stats.correlation`, близкой к ±1.',
    },
    {
      kind: 'multi',
      q: 'Почему индекс может не использоваться?',
      options: [
        'Функция над колонкой в условии: `WHERE date(created_at) = ...`',
        'Нужна большая доля строк — полное чтение дешевле',
        '`LIKE \'%abc\'` — B-дерево не помогает',
        'Устаревшая статистика после массовой загрузки',
        'Индекс слишком маленький',
      ],
      answer: [0, 1, 2, 3],
      explain:
        'Размер индекса ни при чём. Отличить «не подходит» от «считается дорогим» помогает `SET enable_seqscan = off` в сессии.',
    },
    {
      kind: 'order',
      q: 'Нужно построить индекс на большой таблице в проде через goose. Расставьте шаги.',
      items: [
        'Пометить миграцию `-- +goose NO TRANSACTION`',
        'Выполнить `CREATE INDEX CONCURRENTLY`',
        'Проверить, что индекс валиден (построение могло упасть)',
        'Невалидный — удалить и построить заново',
      ],
      explain:
        'Обычный `CREATE INDEX` запрещает запись на всё время построения. CONCURRENTLY не работает внутри транзакции, а инструменты миграций по умолчанию оборачивают миграцию в транзакцию. Упавшее построение оставляет невалидный индекс, который замедляет запись и не используется.',
    },
  ],
} satisfies Quiz
