import type { Quiz } from '../../../components/quiz/types.ts'

export default {
  questions: [
    {
      kind: 'single',
      q: 'Сбор идёт раз в 15 секунд. Какое окно для `rate` на дашборде надёжнее всего?',
      options: ['`[15s]`', '`[30s]`', '`[1m]` или больше, а в Grafana — `$__rate_interval`', '`[1d]`'],
      answer: 2,
      explain:
        'Для скорости нужны минимум две точки в окне. `[15s]` и `[30s]` часто содержат одну — и результат пуст. Правило — не меньше четырёх интервалов сбора; `$__rate_interval` ещё и учитывает шаг графика.',
    },
    {
      kind: 'single',
      q: 'Какой запрос правильно считает общую скорость запросов сервиса из 30 подов?',
      options: [
        '`rate(sum(http_requests_total)[5m:])`',
        '`sum(rate(http_requests_total[5m]))`',
        '`avg(rate(http_requests_total[5m]))`',
        '`sum(http_requests_total) / 300`',
      ],
      answer: 1,
      explain:
        'Сначала `rate` по каждому ряду — тогда перезапуск пода обрабатывается в его собственном ряду, — потом `sum`. `rate` от суммы видит падение суммы при перезапуске любого пода как сброс и рисует ложный пик.',
    },
    {
      kind: 'truefalse',
      q: '`increase` целочисленного счётчика в Prometheus может вернуть дробное число, и это не ошибка.',
      answer: true,
      explain:
        'Prometheus экстраполирует рост на края окна: первая и последняя точки не совпадают с его границами. `rate` и `increase` — оценки, а не учёт.',
    },
    {
      kind: 'single',
      q: 'Запрос `rate(x{status=~"5.."}[5m]) / rate(x[5m])` без агрегации возвращает ровно 1 для каждого ряда. Почему?',
      options: [
        'Все запросы сервиса — ошибки',
        'Ряды сопоставляются по полному набору меток, включая `status`, и каждый ряд ошибок делится сам на себя',
        '`rate` нельзя делить на `rate`',
        'Регулярное выражение `5..` совпадает со всеми кодами',
      ],
      answer: 1,
      explain:
        'Бинарная операция находит пару с теми же метками. Слева `status="500"` — справа тот же ряд с `status="500"`. Метку, по которой делится выборка, убирают: `sum by (route)` с обеих сторон или `ignoring(status)`.',
    },
    {
      kind: 'multi',
      q: 'Что верно про `histogram_quantile`?',
      options: [
        'В `sum by (...)` перед ним обязательно нужно оставить метку `le`',
        'Перцентиль считается интерполяцией внутри бакета — это оценка',
        'Если перцентиль попал в бакет `+Inf`, функция вернёт границу предпоследнего бакета',
        'Его можно брать от summary так же, как от гистограммы',
        'Среднее `histogram_quantile` по подам даёт перцентиль сервиса',
      ],
      answer: [0, 1, 2],
      explain:
        'Без `le` функция не знает бакетов. Внутри бакета она предполагает равномерное распределение. Хвост за последней конечной границей не виден. Складывать нужно бакеты, а не перцентили, и summary для этого не годится.',
    },
    {
      kind: 'single',
      q: 'Какой запрос лучше всего подходит для алерта «диск заполнится в ближайшие 4 часа»?',
      options: [
        '`node_filesystem_avail_bytes < 1e9`',
        '`predict_linear(node_filesystem_avail_bytes[6h], 4 * 3600) < 0`',
        '`rate(node_filesystem_avail_bytes[5m]) < 0`',
        '`irate(node_filesystem_avail_bytes[1m]) < 0`',
      ],
      answer: 1,
      explain:
        '`predict_linear` продлевает тренд за окно и срабатывает заранее, но не трогает диск, который заполнен, но не растёт. `rate` и `irate` — для счётчиков, а свободное место — gauge.',
    },
    {
      kind: 'truefalse',
      q: 'Recording rule пересчитывает историю: после добавления правила его ряд сразу появляется и за прошлые дни.',
      answer: false,
      explain:
        'Правило вычисляется по расписанию «сейчас» и пишет новый ряд только с момента включения. Историю нужно дозаполнять отдельно (например, `promtool tsdb create-blocks-from rules`) или ждать.',
    },
    {
      kind: 'multi',
      q: 'Какие запросы опасны для большого Prometheus?',
      options: [
        '`{__name__=~".+"}` — все ряды всех метрик',
        '`rate(http_request_duration_seconds_bucket[30d])` по сотням тысяч рядов',
        '`up{job="api"}`',
        '`{path=~".*orders.*"}` по метке с миллионом значений',
        '`sum(job:http_requests:rate5m)`',
      ],
      answer: [0, 1, 3],
      explain:
        'Стоимость запроса — ряды × точки. Селектор без имени метрики, долгое окно по сырым гистограммам и регулярка по кардинальной метке трогают огромный объём. `up` одного job и сумма готовой recording rule — дёшевы.',
    },
  ],
} satisfies Quiz
