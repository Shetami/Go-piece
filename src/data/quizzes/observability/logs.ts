import type { Quiz } from '../../../components/quiz/types.ts'

export default {
  questions: [
    {
      kind: 'single',
      q: 'Почему в сервисе на Go логируют через `logger.ErrorContext(ctx, ...)`, а не `logger.Error(...)`?',
      options: [
        'Так запись идёт быстрее',
        'Обработчик получает контекст и может добавить в запись `trace_id` и `span_id`',
        'Без контекста запись не попадёт в stdout',
        'Так уровень ERROR нельзя отключить',
      ],
      answer: 1,
      explain:
        'Контекст трассы живёт в `context.Context`. Свой обработчик `slog` достаёт его в `Handle` и добавляет ID трассы в каждую строку — мост от логов к трассе.',
    },
    {
      kind: 'truefalse',
      q: 'Хорошая практика — логировать ошибку на каждом слое, через который она проходит, чтобы не потерять контекст.',
      answer: false,
      explain:
        '«Залогировать и вернуть» умножает строки на глубину стека и размазывает контекст. Ошибку дополняют контекстом через `fmt.Errorf("...: %w", err)` и логируют один раз — там, где её обрабатывают.',
    },
    {
      kind: 'multi',
      q: 'Где строки логов могут потеряться по пути от сервиса до хранилища?',
      options: [
        'Ротация файлов на узле удалила файл, который агент ещё не дочитал',
        'Буфер агента переполнился, пока хранилище не успевало индексировать',
        'Elasticsearch отверг документ из-за конфликта типа поля',
        'Сервис записал строку в stdout',
        'Агент добавил метаданные Kubernetes',
      ],
      answer: [0, 1, 2],
      explain:
        'Ротация не ждёт агента, буферы конечны, а документ с конфликтом маппинга отвергается целиком. Все три места срабатывают во время аварий, когда логов больше всего, — поэтому у агента и у доставки нужны свои метрики.',
    },
    {
      kind: 'single',
      q: 'Документ записан в Elasticsearch, ответ получен. Когда его найдёт поиск?',
      options: [
        'Сразу после ответа',
        'После ближайшего refresh — по умолчанию в течение секунды',
        'После flush на диск',
        'После слияния сегментов',
      ],
      answer: 1,
      explain:
        'Запись попадает в буфер индексации и translog. Refresh превращает буфер в новый сегмент, и только тогда документ виден поиску — отсюда «почти реальное время». Сохранность до flush обеспечивает translog.',
    },
    {
      kind: 'single',
      q: 'Почему поиск `refus` в поле `message` типа `text` не находит строку «Connection refused»?',
      options: [
        'Поле `text` не индексируется',
        'Инвертированный индекс хранит токены целиком — `connection` и `refused`, а токена `refus` нет',
        'Поиск чувствителен к регистру',
        'Строка ещё не прошла refresh',
      ],
      answer: 1,
      explain:
        'Поиск — это поиск токенов в словаре, а не подстрок. Подстановка `*refus*` перебирала бы весь словарь и работала медленно.',
    },
    {
      kind: 'single',
      q: 'Сервис A пишет поле `user` строкой, сервис B начал писать `user` объектом в тот же поток логов. Что произойдёт с логами B?',
      options: [
        'Поле будет объединено',
        'Документы B с полем `user` будут отвергнуты целиком с `mapper_parsing_exception`',
        'Elasticsearch создаст отдельный индекс для B',
        'Поле `user` у B сохранится строкой',
      ],
      answer: 1,
      explain:
        'Тип поля фиксирует первый документ. Защита — явный шаблон индекса, общая схема полей, `dynamic: false` и `flattened` для произвольных атрибутов.',
    },
    {
      kind: 'truefalse',
      q: 'Тысячи мелких шардов по сотне мегабайт нагружают кластер Elasticsearch сильнее, чем те же данные в шардах по 30 ГБ.',
      answer: true,
      explain:
        'Каждый шард — открытый индекс Lucene с памятью, файлами и записью в состоянии кластера. Ориентир — 10–50 ГБ на шард и rollover по размеру, а не только по дате.',
    },
    {
      kind: 'multi',
      q: 'Что верно про Loki?',
      options: [
        'Он индексирует только метки потока, а строки хранит сжатыми кусками',
        'Поиск по тексту — перебор строк потоков, отобранных селектором',
        '`trace_id` лучше всего сделать меткой потока',
        'Хранение обычно дешевле, чем в Elasticsearch',
        'Он строит инвертированный индекс по каждому слову',
      ],
      answer: [0, 1, 3],
      explain:
        'Loki индексирует минимум и перебирает остальное — поэтому он дешёвый, а скорость поиска зависит от того, насколько узкий селектор. Высококардинальные поля в метках создают тысячи крошечных потоков.',
    },
  ],
} satisfies Quiz
