import type { Quiz } from '../../../components/quiz/types.ts'

export default {
  questions: [
    {
      kind: 'multi',
      q: 'Зачем сервису свой реестр `prometheus.NewRegistry()` вместо глобального?',
      options: [
        'Компоненты получают `Registerer` параметром — явные зависимости',
        'В тестах можно создавать сервис много раз без паники из-за повторной регистрации',
        'Метрики из своего реестра собираются быстрее',
        'Видно и контролируется всё, что отдаёт сервис',
        'Без своего реестра нельзя отдавать метрики по HTTP',
      ],
      answer: [0, 1, 3],
      explain:
        'Свой реестр — это явные зависимости, изолированные тесты и контроль над содержимым `/metrics`. На скорость сбора он не влияет, а глобальный реестр отдаётся через `promhttp.Handler()` так же.',
    },
    {
      kind: 'single',
      q: 'Что класть в метку `route` метрик HTTP?',
      options: [
        '`r.URL.Path` — так точнее',
        'Шаблон маршрута: `r.Pattern` из `ServeMux` или `RoutePattern()` в chi',
        '`r.URL.String()` вместе с параметрами',
        'Ничего: маршрут лучше не хранить',
      ],
      answer: 1,
      explain:
        'Сырой путь даёт по ряду на каждый заказ, а сканер, перебирающий пути, — миллион рядов с 404. Шаблон `GET /orders/{id}` — ограниченный набор значений.',
    },
    {
      kind: 'truefalse',
      q: 'Метод HTTP можно класть в метку как есть: методов всего несколько.',
      answer: false,
      explain:
        'Метод присылает клиент, и это произвольная строка. Всё, что приходит снаружи, нормализуют к известному набору, иначе кардинальность метрик контролирует внешний мир.',
    },
    {
      kind: 'single',
      q: 'Ряд `payment_errors_total{provider="stripe"}` создаётся через `WithLabelValues` при первой ошибке. Почему алерт `increase(payment_errors_total[5m]) > 0` пропустит эту ошибку?',
      options: [
        'Prometheus не собирает ряды, появившиеся между сборами',
        'Первая точка ряда уже равна 1 — роста между точками нет, это начало отсчёта',
        '`increase` не работает для счётчиков с метками',
        'Алерт сработает, пропуска не будет',
      ],
      answer: 1,
      explain:
        'Для `increase` нужен рост между точками внутри окна. Ряда до ошибки не было, первая точка — 1. Важные комбинации меток инициализируют при старте: `WithLabelValues("stripe")` создаёт ряд со значением 0.',
    },
    {
      kind: 'single',
      q: 'Чем свой `Collector` для статистики пула лучше gauge, который обновляет фоновая горутина по таймеру?',
      options: [
        'Он работает без реестра',
        'Значения читаются в момент сбора: всегда свежие, без горутины и без работы, когда сервис не собирают',
        'Он позволяет отдавать строки вместо чисел',
        'Он автоматически добавляет метку `pod`',
      ],
      answer: 1,
      explain:
        '`Collect` вызывается на каждом сборе и спрашивает `pool.Stat()` прямо тогда. Накопительные значения из статистики при этом правильно отдавать как счётчики, чтобы работал `rate`.',
    },
    {
      kind: 'multi',
      q: 'Какие метрики рантайма и процесса помогают увидеть нехватку процессора в контейнере раньше, чем вырастет задержка у пользователей?',
      options: [
        '`go_sched_latencies_seconds` — сколько горутины ждут запуска',
        '`rate(process_cpu_seconds_total[5m])` относительно лимита',
        'время троттлинга из cgroup (cAdvisor)',
        '`process_open_fds`',
        '`go_info`',
      ],
      answer: [0, 1, 2],
      explain:
        'Очередь к процессору внутри Go, реальное потребление против лимита и троттлинг — прямые признаки насыщения процессора. Дескрипторы говорят о незакрытых соединениях, `go_info` — о версии Go.',
    },
    {
      kind: 'truefalse',
      q: 'Exemplar хранит ID трассы рядом с бакетом гистограммы и не создаёт нового ряда.',
      answer: true,
      explain:
        'В отличие от метки `trace_id`, exemplar не часть идентичности ряда: Prometheus держит несколько последних примеров на ряд в отдельном буфере. Нужны формат OpenMetrics и `exemplar-storage`.',
    },
    {
      kind: 'single',
      q: 'Когда в сервисе на Go разумно выбрать OpenTelemetry для метрик вместо `client_golang`?',
      options: [
        'Всегда: `client_golang` устарел',
        'Когда сервис уже пишет трассы через OpenTelemetry и нужен один SDK, общий контекст и независимость от бэкенда',
        'Когда нужна максимальная производительность',
        'Когда метрик меньше десяти',
      ],
      answer: 1,
      explain:
        '`client_golang` проще и зрелее, и для Prometheus его достаточно. OpenTelemetry выигрывает, когда трассы уже на нём: общий контекст, exemplars и атрибуты ресурса из коробки. Смешивать оба для одних и тех же метрик не стоит.',
    },
  ],
} satisfies Quiz
