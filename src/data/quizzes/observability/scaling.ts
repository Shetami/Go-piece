import type { Quiz } from '../../../components/quiz/types.ts'

export default {
  questions: [
    {
      kind: 'single',
      q: 'Для чего годится federation в Prometheus?',
      options: [
        'Чтобы собрать все сырые ряды всех кластеров в один Prometheus',
        'Чтобы собрать в глобальный Prometheus агрегаты из recording rules кластеров',
        'Чтобы реплицировать данные между двумя Prometheus',
        'Чтобы хранить данные годами',
      ],
      answer: 1,
      explain:
        'Один ответ `/federate` на миллионы сырых рядов не масштабируется, и глобальный Prometheus упирается в те же пределы. Federation — для сводки по готовым агрегатам.',
    },
    {
      kind: 'truefalse',
      q: 'Если удалённое хранилище недоступно дольше, чем живёт WAL, remote write потеряет отставшие точки.',
      answer: true,
      explain:
        'Отправитель читает точки из WAL, а WAL обрезается при сбросе head в блок — примерно раз в два часа. Отдельной бесконечной очереди нет, поэтому отставание отправки — отдельный алерт.',
    },
    {
      kind: 'single',
      q: 'Чем агентский режим Prometheus (`--agent`) отличается от обычного?',
      options: [
        'Он собирает метрики с других Prometheus',
        'Он только собирает метрики и отправляет их по remote write — без запросов, правил и долгого хранения',
        'Он хранит данные в объектном хранилище',
        'Он не пишет WAL',
      ],
      answer: 1,
      explain:
        'Агент — лёгкий сборщик: WAL нужен ему только на время отправки. Хранение, запросы и правила — во внешнем хранилище. Ту же роль играют `vmagent`, Alloy и коллектор OpenTelemetry.',
    },
    {
      kind: 'multi',
      q: 'Что верно про Thanos?',
      options: [
        'Sidecar выгружает двухчасовые блоки Prometheus в объектное хранилище',
        'Querier опрашивает sidecar и store gateway веером и убирает дубли реплик',
        'Он снимает ограничение на число рядов в одном Prometheus',
        'Compactor строит прореженные версии старых данных',
        'Он требует перевести все Prometheus на remote write',
      ],
      answer: [0, 1, 3],
      explain:
        'Thanos достраивает к существующим Prometheus глобальный запрос и дешёвую историю в S3, не заменяя их. Поэтому каждый Prometheus по-прежнему ограничен своей машиной, зато переносить ничего не нужно.',
    },
    {
      kind: 'single',
      q: 'Одни и те же данные: `increase(...[1h])` в Prometheus — 3.08, в VictoriaMetrics — 3. Почему?',
      options: [
        'VictoriaMetrics теряет точки при сжатии',
        'MetricsQL не экстраполирует рост на края окна и берёт начало отсчёта из точки перед окном',
        'Prometheus хранит значения с ошибкой округления',
        'Это баг, одно из хранилищ неправо',
      ],
      answer: 1,
      explain:
        'Обе реализации корректны, но считают по-разному. Для графиков разница незаметна, а пороги алертов на коротких окнах при переезде стоит перепроверить.',
    },
    {
      kind: 'truefalse',
      q: 'При превышении `sample_limit` Prometheus сохраняет первые N рядов цели и отбрасывает остальные.',
      answer: false,
      explain:
        'Сбор проваливается целиком: `up == 0`, ни один ряд не записан, ряды цели помечены устаревшими. Лимит спасает Prometheus, но делает сервис невидимым — нужен алерт на `up` и на приближение к лимиту.',
    },
    {
      kind: 'single',
      q: 'Данные старше 10 дней прорежены до разрешения 1 час. Что вернёт `rate(http_requests_total[5m])` за этот период?',
      options: [
        'То же, что и по сырым данным',
        'Пустоту: в пятиминутном окне нет двух точек — окно должно быть больше разрешения',
        'Скорость, умноженную на 12',
        'Ошибку синтаксиса',
      ],
      answer: 1,
      explain:
        'Для `rate` нужны две точки в окне. По прореженным данным окна берут больше разрешения — `[3h]` и шире, — а короткие всплески там уже не видны.',
    },
    {
      kind: 'multi',
      q: 'Нужна разбивка ошибок по каждому из 50 000 клиентов. Что подойдёт?',
      options: [
        'Метка `client_id` на `http_requests_total`',
        'События в колоночном хранилище вроде ClickHouse: одна запись на запрос со всеми полями',
        'Атрибут `client.id` в спанах трасс и поиск по ним',
        'Поле `client_id` в структурированных логах и агрегация в хранилище логов',
        'Отдельная метрика на каждого клиента',
      ],
      answer: [1, 2, 3],
      explain:
        'Метрика — это ряд в памяти на каждое значение метки, и 50 000 клиентов умножают все ряды. Высокая кардинальность — это события: колоночные хранилища, трассы и структурированные логи считают разрез в момент вопроса.',
    },
  ],
} satisfies Quiz
