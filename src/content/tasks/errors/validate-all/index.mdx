---
title: Проверить все поля сразу
description: Валидация, которая сообщает обо всех проблемах одной ошибкой, а не только о первой. errors.Join, свой тип ошибки и errors.As по объединённой ошибке.
kind: implement
topic: errors
level: medium
order: 160
---

Напишите тело `Validate`.

Что проверяют скрытые тесты:

- корректный пользователь → `nil` (настоящий `nil`, а не пустая ошибка);
- пользователь, у которого плохо всё, → одна ошибка, внутри которой три
  `*FieldError` в порядке `Name`, `Email`, `Age`, и текст каждой виден в
  `err.Error()`;
- `errors.As(err, &fe)` находит `*FieldError`;
- возраст 0 допустим.

<Reveal>

```go
func Validate(u User) error {
	var errs []error
	if u.Name == "" {
		errs = append(errs, &FieldError{"Name", "обязательно"})
	}
	if !strings.Contains(u.Email, "@") {
		errs = append(errs, &FieldError{"Email", "нет @"})
	}
	if u.Age < 0 || u.Age > 150 {
		errs = append(errs, &FieldError{"Age", "вне диапазона"})
	}
	return errors.Join(errs...)
}
```

**Пользователю нужны все проблемы сразу.** Форма, которая сообщает
ошибки по одной, заставляет отправлять её трижды.

**`errors.Join` (Go 1.20) делает из списка одну ошибку.** Текст — тексты
частей через перевод строки. `errors.Is` и `errors.As` проходят по всем
частям. И главное для валидации: **`Join` без аргументов или из одних
`nil` возвращает `nil`** — проверять «были ли ошибки» отдельно не надо.

**Ловушка, которой здесь удалось избежать:** своя «мультиошибка» с
методом `Error` и возврат `*MultiError` в `error`, даже когда список пуст.
Вызывающий получает интерфейс с типом и `nil`-указателем внутри —
`err != nil` истинно.

</Reveal>
